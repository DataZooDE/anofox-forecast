---
milestone: v0.9.0
audited: 2026-09-10T00:00:00Z
status: passed
scores:
  requirements: 8/8
  phases: 2/2
  integration: 5/5
  flows: 2/2
gaps: {}
tech_debt:
  - phase: 07-wasm-node-harness-local-green
    items:
      - "Full-suite `--all` run is 2255 pass / 188 fail across 23 files — pre-existing test/sql debt (removed/renamed API refs, DATE+BIGINT casts, distinctness thresholds), NOT WASM/artifact issues. CI gates on the curated 8-file / 396-assertion subset by design. Tracked separately (out-of-scope full-suite triage)."
  - phase: 08-ci-gating-dedicated-workflow-badge
    items:
      - "Live-CI negative-control observation (push → wasm-runtime-test green→red→green in GitHub Actions, badge flip, run URLs) waived by operator on 2026-09-10 on local evidence. Gate exit-code mechanism proven locally (exit 0 → 1 → 0); static CI wiring verified SECURED by integration checker. Confirm on next push to main."
deferred:
  - "WASM-F1: Browser-based (not just Node) WASM E2E harness — future release"
  - "WASM-F2: Shared-memory wasm_threads build — blocked upstream (Rust nightly + -Zbuild-std; duckdb/extension-ci-tools#391 half-fixes)"
---

# Milestone v0.9.0 — WASM Runtime Verification (Phases 7-8): Audit

**Audited:** 2026-09-10
**Status:** ✅ PASSED
**Core value:** Prove the built `anofox_forecast` `.wasm` actually loads and runs in DuckDB-Wasm — not just that it compiles and links — and gate it in CI so WASM regressions fail the build.

## Requirements Coverage (3-source cross-reference)

All 8 v1 requirements satisfied. Each confirmed across VERIFICATION.md status, SUMMARY.md `requirements` frontmatter, and REQUIREMENTS.md traceability (`[x]`).

| REQ-ID | Description | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------------|-------|--------------|---------|--------------|-------|
| WASM-01 | Node harness boots DuckDB-Wasm, serves + LOADs built `.wasm` | 7 | passed | listed | `[x]` | **satisfied** |
| WASM-02 | sqllogictest-subset runner, per-file catalog isolation, `::VARCHAR` DECIMAL formatting | 7 | passed | listed | `[x]` | **satisfied** |
| WASM-03 | Full `test/sql` suite green (curated) with documented skip-list | 7 | passed¹ | listed | `[x]` | **satisfied** |
| DEP-01 | `@duckdb/duckdb-wasm` + `web-worker@1.2.0` pinned to ABI-matching engine | 7 | passed | listed | `[x]` | **satisfied** |
| DEP-02 | `openssl` declared `!wasm32` in `vcpkg.json` | 7 | passed | listed | `[x]` | **satisfied** |
| CI-01 | Gating job `needs:` wasm build, downloads `wasm_eh`, runs harness, fails on WASM error | 8 | passed² | listed | `[x]` | **satisfied** |
| CI-02 | Dedicated WASM workflow separate from main pipeline | 8 | passed | listed | `[x]` | **satisfied** |
| CI-03 | README WASM status badge | 8 | passed | listed | `[x]` | **satisfied** |

¹ WASM-03 accepted with the curated-subset disposition — the 23 `--all` failures are pre-existing test-suite debt (see tech debt), not WASM issues; gate runs the curated green subset.
² CI-01 verification accepted by operator on local evidence; live-CI observation waived (see tech debt).

**Orphaned requirements:** none. Every REQ-ID in the traceability table appears in a phase VERIFICATION.md.

## Phase Verifications

| Phase | Status | Score |
|-------|--------|-------|
| 7 — WASM Node Harness + Local Green | ✅ passed | 4/5 verified live; WASM-03 accepted (out-of-scope test debt) |
| 8 — CI Gating + Dedicated Workflow + Badge | ✅ passed | 3/3 structural + gate mechanism proven locally; live-CI waived by operator |

## Cross-Phase Integration (integration checker: SECURED)

5 cross-phase contracts traced end-to-end, 0 orphaned, 0 missing:

1. **Harness entrypoint** — `MainDistributionPipeline.yml` gating job invokes `node test/wasm/run.mjs --ext "$EXT"` (Phase 7's exact entrypoint); artifact name `anofox_forecast-v1.5.5-extension-wasm_eh` matches ci-tools deploy convention; same-run download via `needs: duckdb-latest-build`.
2. **Curated-subset constraint (LOCKED)** — both workflow invocations are bare `--ext` (no `--all`, no `--file`); `run.mjs` defaults to the 8-file curated list. The 188-failure `--all` debt cannot red the gate.
3. **Dedicated workflow + badge** — `WasmTest.yml` (`name: WASM`) triggers via `workflow_run` on `"Main Extension Distribution Pipeline"`; README badge URL filename matches the workflow filename; cross-run artifact download correct.
4. **Harness contract** — `run.mjs` imports `parseTest`/`runRecords` from `sqllogic.mjs` (both exported); all 8 curated + 4 skip-listed files exist; pinned `@duckdb/duckdb-wasm@1.33.1-dev64.0` + `web-worker@1.2.0` agree across package.json/lock.
5. **vcpkg guard → build** — `openssl` `!wasm32` enables the wasm_eh build the CI job consumes.

**E2E flow:** `vcpkg !wasm32 (DEP-02) → wasm_eh build → artifact upload → download by exact name → npm install pinned deps (DEP-01) → node run.mjs --ext (curated) → non-zero exit gates job (CI-01)` — complete, no breaks. Badge flow: `pipeline completes → workflow_run → cross-run download → same harness → badge.svg (CI-02/CI-03)` — complete.

## Tech Debt (non-blocking, tracked)

- **Full-suite `--all` triage (Phase 7):** 188 failures / 23 files are pre-existing `test/sql` debt (stale/removed API refs, DATE+BIGINT casts, distinctness thresholds), reproduced against a fresh HEAD artifact and absent natively too. Out-of-scope for this CI/infra milestone; gate runs the curated subset by design.
- **Live-CI observation (Phase 8):** waived by operator on local evidence; gate exit-code mechanism proven locally and static wiring verified SECURED. Confirm the green→red→green + badge flip on the next push to main.

## Deferred (future milestones)

- **WASM-F1** — Browser-based (not just Node) WASM E2E harness.
- **WASM-F2** — Shared-memory `wasm_threads` build (blocked upstream: Rust nightly + `-Zbuild-std`; duckdb/extension-ci-tools#391 half-fixes).

## Verdict

All 8 requirements satisfied. Cross-phase integration SECURED. E2E flows complete. No critical gaps. Tech debt documented and tracked. **Milestone v0.9.0 is ready to complete.**

---
phase: 05-explicit-member-ensemble
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - examples/forecasting/ensemble_explicit.sql
  - docs/reference/models/ensemble/ensemble_explicit.md
  - docs/api/07-forecasting.md
autonomous: true
requirements:
  - ENS-02
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The full 26-member allowlist is reachable: each Tier 1/2/3 supported model works as an ensemble member (smoke test) (ENS-02)."
    - "All six combination methods (mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive) produce finite non-NULL yhat for an explicit member list (ENS-02)."
    - "A blocked model (e.g. 'GARCH') named as a member raises a clear InvalidParameter naming that member; an unknown name and a < 2 member list also raise clear errors."
    - "For combination_method := 'mean', ts_forecast_ensemble_by(members := ['AutoARIMA','AutoETS','Theta']) == arithmetic mean of the three members' independent ts_forecast_by forecasts within 1e-6 (ENS-02 DoD, the canonical member set from CONTEXT)."
  artifacts:
    - "examples/forecasting/ensemble_explicit.sql exists, runs clean against the built extension, and exercises the DoD cross-check + six-method smoke + error tests"
    - "docs/reference/models/ensemble/ensemble_explicit.md exists"
    - "docs/api/07-forecasting.md contains a ts_forecast_ensemble_by entry"
  key_links:
    - "Every doc SQL snippet is run through the built extension (PR #230 rule) — no eyeballed examples."
    - "The 26 supported members pass through build_forecaster; the 10 blocked members return InvalidParameter surfaced to SQL as an error."
---

<objective>
Expand the proven tracer (05-01) to the full ENS-02 surface: exercise the canonical `['AutoARIMA','AutoETS','Theta']` Mean cross-check, smoke-test all six combination methods and a representative sample of the 26-member allowlist, verify the error paths (unknown name, < 2 members, blocked model), and document the surface. No new source wiring — 05-01 already made the full 36-variant `build_forecaster` match compile; this plan proves the broadened runtime coverage and writes docs.

Purpose: Deliver the complete ENS-02 Definition of Done — a verified runnable example covering the canonical member set + all methods + error handling, plus reference and API docs whose every snippet is run through the built extension.

Output: `examples/forecasting/ensemble_explicit.sql` (full DoD), `docs/reference/models/ensemble/ensemble_explicit.md`, and a `ts_forecast_ensemble_by` entry in `docs/api/07-forecasting.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-explicit-member-ensemble/05-CONTEXT.md
@.planning/phases/05-explicit-member-ensemble/05-RESEARCH.md
@.planning/phases/05-explicit-member-ensemble/05-PATTERNS.md
@.planning/phases/05-explicit-member-ensemble/05-01-PLAN.md
</context>

## Artifacts this phase produces (this plan)

- `examples/forecasting/ensemble_explicit.sql` — full DoD example (canonical `['AutoARIMA','AutoETS','Theta']` Mean cross-check, six-method smoke, member-allowlist sample, error tests). This is the milestone-canonical example; the tracer example `ensemble_explicit_tracer.sql` from 05-01 may be kept or superseded — if kept, it stays green.
- `docs/reference/models/ensemble/ensemble_explicit.md` — reference doc for the surface (analog: `docs/reference/models/ensemble/autoensemble.md`).
- `docs/api/07-forecasting.md` — new `ts_forecast_ensemble_by` API entry.

No changes to Rust core, FFI, C++, macros, or CMakeLists — 05-01 delivered all wiring. If the broadened smoke test surfaces a constructor bug in a `build_forecaster` arm (e.g. a wrong constructor for `DynamicOptimizedTheta`/`SESOptimized`/`SeasonalESOptimized`/`ETS` flagged in RESEARCH Assumptions A2-A4/A7), fix it in `crates/anofox-fcst-core/src/forecast.rs` and note the deviation in the SUMMARY — that is an allowed corrective edit, not new scope.

<tasks>

<task type="auto">
  <name>Task 1: Full DoD example — canonical Mean cross-check + six-method smoke + member-allowlist sample + error tests</name>
  <files>examples/forecasting/ensemble_explicit.sql</files>
  <read_first>
    - examples/forecasting/ensemble_explicit_tracer.sql (created in 05-01 — reuse its clean-series setup + cross-check + assertion structure).
    - examples/forecasting/autoensemble.sql (whole file — the `.print` section headers, six-method smoke, and assertion-returns-0 idiom to mirror).
    - 05-RESEARCH.md lines 694-761 (cross-check SQL + Requirements→Test map: Section 1 cross-check, Section 2 six-method smoke, Section 3 error tests), lines 149-161 (Tier 1/2/3 supported set + blocked set).
    - 05-CONTEXT.md lines 59-60 (DoD: canonical `['AutoARIMA','AutoETS','Theta']` Mean cross-check).
  </read_first>
  <action>
    Create `examples/forecasting/ensemble_explicit.sql` with a header comment (title, ENS-02, run command `./build/release/duckdb -unsigned < examples/forecasting/ensemble_explicit.sql`) and `LOAD anofox_forecast;`, then:

    **Section 1 — DoD Mean cross-check (canonical member set).** Build a clean 60-obs linearly-trended series (id=1, ds = '2020-01-01'::DATE + (i-1) days, y = 10.0 + i*0.5) via `range(1,61)`. Compute `ens_result` = `ts_forecast_ensemble_by('ae_test', id, ds, y, ['AutoARIMA','AutoETS','Theta'], 5, '1d', combination_method := 'mean', seasonal_period := 0)`. Compute three member tables via `ts_forecast_by(... 'AutoARIMA' / 'AutoETS' / 'Theta' ...)`. Compute `m_manual = (y_arima + y_ets + y_theta)/3.0` joined on `forecast_step`. Emit a cross-check select (forecast_step, ens_mean, manual_mean, diff, `abs(diff) < 1e-6 AS match`). Emit the assertion `SELECT count(*) AS mismatch_count FROM ens_result e JOIN m_manual m USING (forecast_step) WHERE abs(e.yhat - m.manual_mean) >= 1e-6;` — MUST return 0. Also assert `yhat_lower`/`yhat_upper` are NULL. (This is the canonical CONTEXT DoD member set; if Theta selection makes the exact match brittle on this platform, keep the tracer's `Naive` variant as a second cross-check section rather than weakening the assertion.)

    **Section 2 — six-method smoke.** For methods `mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive`, run `ts_forecast_ensemble_by('ae_test', id, ds, y, ['AutoARIMA','AutoETS','Theta'], 5, '1d', combination_method := <m>, seasonal_period := 0)` and assert every `yhat` is finite and non-NULL (e.g. a query that counts rows where `yhat IS NULL OR NOT isfinite(yhat)` and expects 0), and that `yhat_lower`/`yhat_upper` are NULL (point-only in Phase 5).

    **Section 3 — member-allowlist sample.** Exercise a representative slice of the 26-member allowlist across tiers to prove they are reachable as members: e.g. `['Naive','SES']`, `['AutoARIMA','Theta','Holt']`, `['CrostonClassic','ADIDA','IMAPA']`, and a seasonal set `['SeasonalNaive','HoltWinters']` with `seasonal_period := 12`. Assert each returns finite non-NULL `yhat`. (Full 26-way enumeration is not required — a per-tier sample proves the factory path; the exhaustive-match compile guarantee from 05-01 covers the rest.)

    **Section 4 — error tests (each must raise, not silently succeed).** Demonstrate: (a) unknown member name `['AutoARIMA','NotAModel']` → error naming `NotAModel`; (b) fewer than 2 members `['AutoARIMA']` → "at least 2 members" error; (c) blocked model `['AutoARIMA','GARCH']` → InvalidParameter naming `GARCH` with an alternative suggestion; (d) NULL/empty members. Since a raising statement aborts a piped script, structure these so each error is demonstrated (e.g. separate clearly-commented statements, or note in comments that these are expected-error demonstrations) — follow whatever idiom autoensemble.sql / other examples use for expected-error cases; if the project has no in-script error-capture idiom, place the four error statements at the END under a `-- EXPECTED ERRORS` banner and document the expected message for each so a reviewer running them one-by-one sees the correct error.

    Run the whole example against the BUILT extension (`./build/release/duckdb -unsigned < examples/forecasting/ensemble_explicit.sql`). Section 1's `mismatch_count` MUST be 0; Sections 2-3 MUST show 0 NULL/non-finite yhat; Section 4 MUST raise the four expected errors. If any `build_forecaster` arm errors at runtime due to a wrong constructor (RESEARCH A2-A4/A7), fix it in forecast.rs, rebuild, and re-run.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && ./build/release/duckdb -unsigned < examples/forecasting/ensemble_explicit.sql 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `examples/forecasting/ensemble_explicit.sql` exists and runs against the built extension.
    - Section 1 assertion `mismatch_count` returns exactly 0 for `['AutoARIMA','AutoETS','Theta']` with `combination_method := 'mean'` (DoD cross-check within 1e-6).
    - Section 2: all six combination methods return finite, non-NULL `yhat`; `yhat_lower`/`yhat_upper` are NULL.
    - Section 3: the sampled members across Tier 1/2/3 (including a seasonal set with `seasonal_period := 12`) each return finite non-NULL `yhat`.
    - Section 4: `['AutoARIMA','NotAModel']`, `['AutoARIMA']`, and `['AutoARIMA','GARCH']` each raise an error; the GARCH error names `GARCH`; the unknown-name error names `NotAModel`; the < 2 error mentions the 2-member minimum.
  </acceptance_criteria>
  <done>The full ENS-02 DoD is demonstrated by a runnable, verified example: canonical Mean cross-check passes, all six methods and a member-allowlist sample produce valid forecasts, and the three error paths raise clear messages.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation — reference doc + API entry, every snippet verified against the built extension</name>
  <files>docs/reference/models/ensemble/ensemble_explicit.md, docs/api/07-forecasting.md</files>
  <read_first>
    - docs/reference/models/ensemble/autoensemble.md (whole file — structure, tone, section layout to mirror for the new reference doc).
    - docs/api/07-forecasting.md (find the AutoEnsemble / ts_forecast_by entries — mirror the entry format for the new `ts_forecast_ensemble_by` row/section).
    - examples/forecasting/ensemble_explicit.sql (Task 1 — source every doc snippet from statements that were actually run and verified).
    - 05-CONTEXT.md lines 26-34 (params/defaults, min 2 members, duplicates allowed), 05-RESEARCH.md lines 149-161 (supported/blocked member lists), lines 670-676 (seasonal_period=0 fallback behavior to document).
  </read_first>
  <action>
    Create `docs/reference/models/ensemble/ensemble_explicit.md` mirroring `autoensemble.md`: describe `ts_forecast_ensemble_by(source, group_col, date_col, target_col, members, horizon, frequency, combination_method := 'mean', seasonal_period := 0)`; document the `members VARCHAR[]` param (2+ required, duplicates allowed, uses the same model names as `ts_forecast_by`); list the 26 SUPPORTED members (Tier 1/2/3 from RESEARCH) and the 10 BLOCKED members (GARCH, Laplace, ARIMA, MFLES, AutoMFLES, MSTL, AutoMSTL, TBATS, AutoTBATS, AutoEnsemble) with the reason each is blocked and the suggested alternative; document the six combination methods (default Mean); document that `yhat_lower`/`yhat_upper` are NULL in Phase 5 (ensemble intervals are Phase 6 / EPI-01) and point to Phase 6 for introspection; document the `seasonal_period=0` → seasonal-model-fallback-to-12 behavior (RESEARCH Pitfall 5). Include the Mean cross-check as the worked example.

    Add a `ts_forecast_ensemble_by` entry to `docs/api/07-forecasting.md` in the same format as the existing `ts_forecast_by` / AutoEnsemble entries (signature, param table, one runnable example, cross-reference to the reference doc).

    **PR #230 rule (BLOCKING):** every SQL snippet you put in either doc MUST be copy-run through the built extension (`./build/release/duckdb -unsigned`) and produce the shown output. Do not paste any snippet you have not executed. Prefer lifting snippets verbatim from the verified `ensemble_explicit.sql`.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && test -f docs/reference/models/ensemble/ensemble_explicit.md && grep -c 'ts_forecast_ensemble_by' docs/reference/models/ensemble/ensemble_explicit.md && grep -c 'ts_forecast_ensemble_by' docs/api/07-forecasting.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/models/ensemble/ensemble_explicit.md` exists and contains `ts_forecast_ensemble_by` and both the 26-supported and 10-blocked member lists.
    - `docs/api/07-forecasting.md` contains a `ts_forecast_ensemble_by` entry (>= 1).
    - Every SQL snippet in both docs has been run through the built extension and matches shown output (PR #230) — snippets are lifted from the verified example where possible.
    - Docs state `yhat_lower`/`yhat_upper` are NULL in Phase 5 and reference Phase 6 (EPI-01/INSP-01) for intervals + introspection.
  </acceptance_criteria>
  <done>Reference and API docs for ts_forecast_ensemble_by exist, list the supported/blocked members and six methods, and every snippet is verified against the built extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL user → ts_forecast_ensemble_by | User-supplied member list / method / period — same boundary as 05-01; this plan adds no new code path, only exercises and documents it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Documentation examples | low | accept | Docs use synthetic in-memory series only (no real/PII data); rationale: examples build their own `range()` data. |
| T-05-06 | Tampering | Doc snippets drift from actual behavior | low | mitigate | PR #230 rule enforced (Task 2): every snippet run through the built extension before it ships; no eyeballed examples. |

No new source code, no new packages, no new trust boundary vs 05-01. All 05-01 mitigations (buffer-length bounding, ≥2-member validation, catch_unwind panic safety, closed-enum member whitelist) remain in force. ASVS L1 block-on-high: no high-severity threats.
</threat_model>

<verification>
- `examples/forecasting/ensemble_explicit.sql` runs clean: Section 1 `mismatch_count = 0`, Sections 2-3 zero NULL/non-finite yhat, Section 4 raises the three expected errors.
- `docs/reference/models/ensemble/ensemble_explicit.md` and the `ts_forecast_ensemble_by` entry in `docs/api/07-forecasting.md` exist; every snippet verified against the built extension.
</verification>

<success_criteria>
- ENS-02 DoD fully met: canonical `['AutoARIMA','AutoETS','Theta']` Mean cross-check passes within 1e-6 via a verified example; all six methods and a member-allowlist sample produce valid forecasts; the unknown-name / < 2-member / blocked-model (GARCH) error paths each raise clear messages.
- Reference + API docs shipped with only verified snippets.
</success_criteria>

<output>
Create `.planning/phases/05-explicit-member-ensemble/05-02-SUMMARY.md` when done.
</output>

---
phase: 05-explicit-member-ensemble
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anofox-fcst-core/src/forecast.rs
  - crates/anofox-fcst-ffi/src/lib.rs
  - src/include/anofox_fcst_ffi.h
  - src/table_functions/ts_forecast_ensemble_native.hpp
  - src/table_functions/ts_forecast_ensemble_native.cpp
  - CMakeLists.txt
  - src/anofox_forecast_extension.cpp
  - src/macros/ts_macros.cpp
  - examples/forecasting/ensemble_explicit_tracer.sql
autonomous: true
requirements:
  - ENS-02
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user can call ts_forecast_ensemble_by with an explicit member list of 2+ models and combination_method := 'mean' and get one blended forecast row-set per series back (ENS-02, tracer scope)."
    - "For combination_method := 'mean', the ensemble yhat equals the arithmetic mean of the same members' independent ts_forecast_by forecasts within 1e-6 (ENS-02 DoD cross-check)."
    - "yhat_lower and yhat_upper are NULL (point forecasts only; intervals deferred to Phase 6 EPI-01)."
  artifacts:
    - "crates/anofox-fcst-core/src/forecast.rs contains fn build_forecaster( and fn forecast_explicit_ensemble("
    - "crates/anofox-fcst-ffi/src/lib.rs contains anofox_ts_forecast_ensemble"
    - "src/include/anofox_fcst_ffi.h contains anofox_ts_forecast_ensemble"
    - "src/table_functions/ts_forecast_ensemble_native.cpp exists and is registered"
    - "CMakeLists.txt contains ts_forecast_ensemble_native.cpp"
    - "src/macros/ts_macros.cpp contains ts_forecast_ensemble_by"
    - "examples/forecasting/ensemble_explicit_tracer.sql exists and runs clean against the built extension"
  key_links:
    - "SQL macro ts_forecast_ensemble_by -> new native/scalar function -> FFI anofox_ts_forecast_ensemble -> core forecast_explicit_ensemble -> build_forecaster per member -> Ensemble::new(members).with_method(...)."
    - "member VARCHAR[] -> null-delimited members_buf + members_buf_len across the FFI boundary; Rust splits on NUL to recover member names."
    - "New .cpp file MUST be in CMakeLists EXTENSION_SOURCES AND registered in extension.cpp or the SQL function is absent at runtime."
---

<objective>
Wire the explicit-member ensemble end-to-end as ONE thin vertical tracer slice: a user names 2+ member models plus a combination method, the extension fits each named member per series and combines them via the crate `Ensemble`, and the Mean cross-check proves the blend equals the arithmetic mean of the members' independent forecasts.

This is the tracer: prove the full path core→FFI→C++→macro→example on the two best-known members (`AutoARIMA`, `AutoETS`) plus one baseline, with `combination_method := 'mean'`. Plan 05-02 expands to the full 26-member allowlist + all six methods + docs.

Purpose: De-risk the two genuinely new pieces in one commit chain — (1) the name→`Box<dyn Forecaster>` factory (`build_forecaster`) and (2) the null-delimited `VARCHAR[]` FFI marshalling — before broadening coverage. Delivers ENS-02's DoD cross-check.

Output: `build_forecaster` + `forecast_explicit_ensemble` in Rust core; `anofox_ts_forecast_ensemble` FFI export (+ regenerated header); new `ts_forecast_ensemble_native.{hpp,cpp}` table/scalar function added to CMakeLists + registered; `ts_forecast_ensemble_by` macro; a verified tracer example.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-explicit-member-ensemble/05-CONTEXT.md
@.planning/phases/05-explicit-member-ensemble/05-RESEARCH.md
@.planning/phases/05-explicit-member-ensemble/05-PATTERNS.md
@.planning/phases/04-autoensemble-surface-combination-methods/04-01-SUMMARY.md
</context>

## Artifacts this phase produces (tracer scope — full set completed in 05-02)

New Rust core symbols (`crates/anofox-fcst-core/src/forecast.rs`):
- `build_forecaster(model_type: ModelType, period: Option<usize>) -> Result<BoxedForecaster>` — compiler-exhaustive match over all 36 `ModelType` variants; the 10 blocked variants return `Err(InvalidParameter)`.
- `forecast_explicit_ensemble(values: &[f64], horizon: usize, member_names: &[String], method_str: Option<&str>, period: usize) -> Result<ForecastOutput>`.

New FFI symbol (`crates/anofox-fcst-ffi/src/lib.rs`):
- `anofox_ts_forecast_ensemble(values, validity, length, members_buf, members_buf_len, members_count, combination_method, seasonal_period, horizon, out_result, out_error) -> bool` (+ header symbol in `src/include/anofox_fcst_ffi.h` via `make header`).

New C++ symbols:
- `src/table_functions/ts_forecast_ensemble_native.hpp` — declares `RegisterTsForecastEnsembleNativeFunction(ExtensionLoader &loader)`.
- `src/table_functions/ts_forecast_ensemble_native.cpp` — implements the function + registration.
- CMakeLists.txt `EXTENSION_SOURCES` entry for `ts_forecast_ensemble_native.cpp`.
- `#include` + `RegisterTsForecastEnsembleNativeFunction(loader)` in `src/anofox_forecast_extension.cpp`.

New macro (`src/macros/ts_macros.cpp`):
- `ts_forecast_ensemble_by(source, group_col, date_col, target_col, members, horizon, frequency, combination_method := '', seasonal_period := 0)`.

New example: `examples/forecasting/ensemble_explicit_tracer.sql`.

Full 26-member allowlist, all six methods, error tests, and docs are produced in Plan 05-02.

<critical_dispatch_note>
**READ BEFORE WRITING THE C++ / MACRO.** The RESEARCH and PATTERNS sketches show the macro calling `unnest(_ts_forecast_ensemble_native(group_col, LIST(...), LIST(...), members, ...), recursive := true)`. That is a **scalar**-shaped call. But `ts_forecast_native.cpp`'s `_ts_forecast_native` is a TABLE-in-out function whose first arg is `LogicalType::TABLE` — it does NOT match that macro call.

The established per-series `_by` macro pattern (`ts_forecast_by`, ts_macros.cpp:575) actually calls **`_ts_forecast_scalar`** (a `ScalarFunction`, `src/scalar_functions/ts_forecast_scalar.cpp`) with signature `(dates LIST, values LIST(DOUBLE), horizon INT, frequency VARCHAR, method VARCHAR, params ANY)` and wraps it in `unnest(..., recursive := true)` under `GROUP BY group_col`.

**Decision (Claude's discretion, per CONTEXT):** implement the new function as a **scalar** function following the `_ts_forecast_scalar` precedent — it is the proven per-series `_by` shape and matches the macro sketch's `unnest(...)` call. Keep the source file named `src/table_functions/ts_forecast_ensemble_native.cpp` (the project's BLOCKING CMakeLists rule and PATTERNS both name that file), and name the SQL function `_ts_forecast_ensemble_native`, but register it via `ScalarFunction` (like `_ts_forecast_scalar`), NOT `TableFunction`. If during the tracer you find the table-in-out shape is required instead, align the macro's call accordingly and record the choice in the SUMMARY — the DoD cross-check is the arbiter, not the internal shape.
</critical_dispatch_note>

<tasks>

<task type="tracer">
  <name>Task 1: Rust core — build_forecaster factory + forecast_explicit_ensemble (tracer members only, full-match skeleton)</name>
  <files>crates/anofox-fcst-core/src/forecast.rs</files>
  <read_first>
    - crates/anofox-fcst-core/src/forecast.rs lines 1-22 (existing imports — AutoARIMA/AutoETS/Theta/etc. already imported), lines 170-290 (`ModelType`, `from_str`, `name()`), lines 2377-2409 (`extract_forecast`), lines 2532-2550 (`parse_combination_method` — REUSE verbatim, do not duplicate), lines 2560-2581 (`forecast_auto_ensemble` — mirror its structure).
    - Before writing each match arm, read the corresponding inline `forecast_*` sub-function (forecast.rs ~lines 1240-2460) to copy the EXACT constructor. PATTERNS.md lines 103-128 lists which arm maps to which sub-fn. In particular verify inline: `DynamicOptimizedTheta` (is it `DynamicTheta::new_dotm()`?), `SESOptimized`, `SeasonalESOptimized`, and the `ETS` seasonal-period setter — RESEARCH Open Questions 2-3 and Assumptions A2-A4/A7 flag these as unconfirmed.
    - RESEARCH.md lines 98-285 (`build_forecaster` skeleton + supported/blocked table), lines 308-353 (`forecast_explicit_ensemble` skeleton).
    - 05-PATTERNS.md lines 28-160 (forecast.rs additive placement + import guidance).
  </read_first>
  <action>
    Add two functions to forecast.rs after `forecast_auto_ensemble` (~line 2581), additive only (no existing code changes).

    (1) `pub(crate) fn build_forecaster(model_type: ModelType, period: Option<usize>) -> Result<BoxedForecaster>` — a `match model_type { ... }` that is COMPILER-EXHAUSTIVE over ALL 36 `ModelType` variants (no wildcard `_ =>` arm; a wildcard would defeat the future-safety guarantee). Box each SUPPORTED member's constructor using the exact constructor confirmed by reading its inline `forecast_*` sub-fn. The 10 BLOCKED variants (`GARCH`, `Laplace`, `ARIMA`, `MFLES`, `AutoMFLES`, `MSTL`, `AutoMSTL`, `TBATS`, `AutoTBATS`, `AutoEnsemble`) each return `Err(ForecastError::InvalidParameter { param: "members".into(), value: <the member name via model_type.name()>, reason: <names the member + suggests an alternative> })`. Use `model_type.name()` for the value so the error names the offending member. For seasonal-only models (`SeasonalNaive`, `HoltWinters`, `SeasonalES`, `SeasonalESOptimized`, `SeasonalWindowAverage`) fall back to period 12 when `period` is 0/1 (documented behavior; do not error). Add any needed local `use` (`BoxedForecaster`, `baseline::{Naive, RandomWalkWithDrift, SeasonalNaive, SeasonalWindowAverage, WindowAverage}`, `ensemble::Ensemble`) — check which baseline types are already imported at file top vs imported locally in the inline `forecast_*` fns before adding, to avoid duplicate-import errors.

    (2) `fn forecast_explicit_ensemble(values: &[f64], horizon: usize, member_names: &[String], method_str: Option<&str>, period: usize) -> Result<ForecastOutput>` mirroring `forecast_auto_ensemble`: (a) if `member_names.len() < 2` return `Err(InvalidParameter { param: "members", value: len, reason: "at least 2 members are required for an ensemble" })`; (b) `let combination_method = parse_combination_method(method_str)?` (REUSE Phase-4 fn); (c) for each name, `name.parse::<ModelType>()` mapping parse failure to `InvalidParameter` naming the unknown member, then `build_forecaster(model_type, if period > 1 { Some(period) } else { None })?`, collecting `Vec<Box<dyn Forecaster>>`; (d) `let ts = make_timeseries(values)?;` then `let mut ens = Ensemble::new(members).with_method(combination_method); ens.fit(&ts).map_err(|e| ComputationError(...))?;`; (e) return `extract_forecast(&ens, horizon, "Ensemble")`. VERIFY the crate `Ensemble` struct implements `Forecaster` (RESEARCH Open Q1 / Assumption A1) by reading `anofox-forecast-0.15.3/src/models/ensemble/model.rs` — if `Ensemble` does NOT `impl Forecaster`, call `ens.predict(horizon)?` directly and build the `ForecastOutput` inline (point vector; `lower: vec![]`, `upper: vec![]`; `model_name: "Ensemble"`). `model_name` MUST be plain `"Ensemble"` (per CONTEXT D — mirrors Phase 4's plain `AutoEnsemble`).

    Tracer scope: the match must compile-cover all 36 variants, but you only need to have EXERCISED (via Task 3's example) `AutoARIMA`, `AutoETS`, and `Naive`. Getting the other supported arms compiling is required (exhaustive match); their runtime correctness is fully exercised in Plan 05-02's smoke test.
  </action>
  <verify>
    <automated>cd crates/anofox-fcst-core && cargo build -p anofox-fcst-core 2>&1 | tail -5 && grep -c 'fn build_forecaster(' src/forecast.rs && grep -c 'fn forecast_explicit_ensemble(' src/forecast.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-fcst-core` exits 0 (clean compile — exhaustive match confirmed by the compiler, no wildcard arm).
    - `crates/anofox-fcst-core/src/forecast.rs` contains `fn build_forecaster(` exactly once.
    - `crates/anofox-fcst-core/src/forecast.rs` contains `fn forecast_explicit_ensemble(` exactly once.
    - `forecast.rs` build_forecaster body contains an `InvalidParameter` arm for each of GARCH, Laplace, ARIMA, MFLES, AutoMFLES, MSTL, AutoMSTL, TBATS, AutoTBATS, AutoEnsemble (grep: `grep -c 'ModelType::GARCH' src/forecast.rs` >= 1 and the reason strings name the member).
    - `forecast_explicit_ensemble` calls `parse_combination_method` (reuse, not re-implemented) — `grep -c 'fn parse_combination_method' src/forecast.rs` stays 1.
    - `model_name` passed to `extract_forecast`/`ForecastOutput` is the literal `"Ensemble"`.
  </acceptance_criteria>
  <done>build_forecaster (exhaustive 36-variant match, 10 blocked arms) and forecast_explicit_ensemble compile clean; the crate Ensemble Forecaster question is resolved and the code path uses whichever construction is correct.</done>
</task>

<task type="auto">
  <name>Task 2: FFI export anofox_ts_forecast_ensemble (null-delimited member buffer) + make header</name>
  <files>crates/anofox-fcst-ffi/src/lib.rs, src/include/anofox_fcst_ffi.h</files>
  <read_first>
    - crates/anofox-fcst-ffi/src/lib.rs lines 3386-3612 (`anofox_ts_forecast` — the exact analog: signature, null-pointer guard, `catch_unwind` + `build_series`, `CStr`→`Option<String>` parse at ~3452-3457, `ForecastResult` marshalling at ~3492-3583, error handling at ~3585-3612).
    - 05-RESEARCH.md lines 373-434 (FFI marshalling shape + why null-delimited + `make header`).
    - 05-PATTERNS.md lines 163-257 (FFI additive placement, null-delimited parse, ForecastResult marshalling — copy verbatim).
    - Makefile line 29 (`header` target).
  </read_first>
  <action>
    Append a new `#[no_mangle] pub unsafe extern "C" fn anofox_ts_forecast_ensemble(...)` AFTER the last existing extern fn in lib.rs (do not insert inside another fn). Signature (append `members_buf_len` explicitly so Rust never has to scan for the buffer length — RESEARCH/PATTERNS both recommend this):
    `values: *const c_double, validity: *const u64, length: size_t, members_buf: *const c_char, members_buf_len: size_t, members_count: size_t, combination_method: *const c_char, seasonal_period: c_int, horizon: c_int, out_result: *mut ForecastResult, out_error: *mut AnofoxError` returning `bool`.

    Body mirrors `anofox_ts_forecast`: (1) init `*out_error = AnofoxError::success()`; null-guard `values.is_null() || members_buf.is_null() || out_result.is_null()` → set `ErrorCode::NullPointer`, return false. (2) Inside `catch_unwind(AssertUnwindSafe(|| { ... }))`: build the values series via the existing `build_series(values, validity, length)` helper; recover the member list from the buffer using the explicit length — `let bytes = std::slice::from_raw_parts(members_buf as *const u8, members_buf_len); let member_names: Vec<String> = bytes.split(|&b| b == 0).filter(|s| !s.is_empty()).map(|s| String::from_utf8_lossy(s).into_owned()).collect();`. Do NOT over-read: bound strictly by `members_buf_len`. Optionally assert `member_names.len() == members_count`; if they differ, return `Err(InvalidParameter { param: "members", ... })` (defensive — mismatch means a marshalling bug). Parse `combination_method` via the same `CStr`→`Option<&str>`/`Option<String>` pattern used for `kalman_model` (null or empty → `None`). Call `anofox_fcst_core::forecast_explicit_ensemble(&values_vec, horizon as usize, &member_names, method_opt.as_deref(), seasonal_period as usize)`. (3) Marshal the `Ok(Ok(output))` case exactly like `anofox_ts_forecast`: `alloc_or_error` for `point_forecasts`; set `lower_bounds`/`upper_bounds` to `ptr::null_mut()` (point-only — NULL intervals, matching AutoEnsemble); `copy_string_to_buffer(&output.model_name, &mut (*out_result).model_name)`. (4) `Ok(Err(e))` → `(*out_error).set_error(e.to_code()/appropriate ErrorCode, &e.to_string()); false`. `Err(_panic)` → `ErrorCode::Panic`; false. Match the existing error-code mapping so `InvalidParameter` surfaces as an invalid-input code the C++ layer converts to `InvalidInputException`.

    Then run `make header` to regenerate `src/include/anofox_fcst_ffi.h` via cbindgen. Confirm the new function + its params appear in the header.
  </action>
  <verify>
    <automated>cd crates/anofox-fcst-ffi && cargo build -p anofox-fcst-ffi 2>&1 | tail -3 && cd /home/simonm/projects/duckdb/anofox-forecast && make header >/dev/null 2>&1; grep -c 'anofox_ts_forecast_ensemble' src/include/anofox_fcst_ffi.h && grep -c 'members_buf_len' src/include/anofox_fcst_ffi.h</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-fcst-ffi` exits 0.
    - `crates/anofox-fcst-ffi/src/lib.rs` contains `anofox_ts_forecast_ensemble` exactly once (the extern fn definition).
    - `src/include/anofox_fcst_ffi.h` contains `anofox_ts_forecast_ensemble` (>= 1) after `make header`.
    - `src/include/anofox_fcst_ffi.h` contains `members_buf_len` (the explicit-length param is present in the generated header).
    - The member-list recovery uses `members_buf_len` to bound the slice (no unbounded scan / no over-read past the buffer).
  </acceptance_criteria>
  <done>anofox_ts_forecast_ensemble compiles, is panic-safe (catch_unwind), bounds the member buffer by explicit length, emits NULL intervals, and is declared in the regenerated C header.</done>
</task>

<task type="auto">
  <name>Task 3: C++ function + CMakeLists + registration + macro + verified Mean cross-check tracer example</name>
  <files>src/table_functions/ts_forecast_ensemble_native.hpp, src/table_functions/ts_forecast_ensemble_native.cpp, CMakeLists.txt, src/anofox_forecast_extension.cpp, src/macros/ts_macros.cpp, examples/forecasting/ensemble_explicit_tracer.sql</files>
  <read_first>
    - <critical_dispatch_note> at the top of this plan (scalar vs table-in-out decision — follow the `_ts_forecast_scalar` precedent).
    - src/scalar_functions/ts_forecast_scalar.cpp: header comment lines 19-26 (call shape: `(dates LIST, values LIST, horizon INT, frequency VARCHAR, method VARCHAR, params ANY)`), lines 574-590 (`RegisterTsForecastScalarFunction` + `ScalarFunction` registration), and the body that joins into the FFI call + emits the result struct. This is the closest analog for a per-series `_by` function.
    - src/table_functions/ts_forecast_native.cpp lines 1-14 (includes), the FFI-call + error-handling block (~lines 697-703: `INVALID_MODEL`/`INVALID_INPUT` → `InvalidInputException`, else skip), and the null/NULL-interval output handling — reuse the error-code→exception mapping.
    - src/macros/ts_macros.cpp lines 575-594 (`ts_forecast_by` macro — copy structure; `members` replaces `method`; add `combination_method`/`seasonal_period` named params).
    - src/anofox_forecast_extension.cpp lines 5-6 (`#include` block) and lines 176-178 (Register* call block).
    - CMakeLists.txt lines 178-180 (forecast native source entries — add the new file right after `ts_forecast_var_native.cpp`).
    - examples/forecasting/autoensemble.sql (whole file — the Mean cross-check + `.print` + assertion structure to mirror).
    - 05-RESEARCH.md lines 694-739 (cross-check SQL), 05-PATTERNS.md lines 261-529 (C++ + macro + CMakeLists specifics).
  </read_first>
  <action>
    Create the new function following the `_ts_forecast_scalar` precedent (scalar shape), named SQL function `_ts_forecast_ensemble_native`, in file `src/table_functions/ts_forecast_ensemble_native.cpp` (+ `.hpp` declaring `RegisterTsForecastEnsembleNativeFunction(ExtensionLoader &loader)`). Registration call shape: `(dates LIST(TIMESTAMP-or-ANY), values LIST(DOUBLE), members LIST(VARCHAR), horizon INTEGER, frequency VARCHAR, combination_method VARCHAR, seasonal_period INTEGER)` returning the same STRUCT/row shape as `_ts_forecast_scalar` (forecast_step, ds, yhat, yhat_lower, yhat_upper, model_name) so `unnest(..., recursive := true)` works in the macro. In the function body: extract the `members` LIST(VARCHAR) into a `std::vector<std::string>`, skipping NULL children; if `< 2` members, throw `InvalidInputException("ts_forecast_ensemble_by: at least 2 members are required")` (also guard NULL list). Build the null-delimited buffer: `std::string members_buf; for (auto &n : member_names) { members_buf += n; members_buf += '\0'; }`; pass `members_buf.data()`, `members_buf.size()` (as `members_buf_len`), and `member_names.size()` (as `members_count`) to `anofox_ts_forecast_ensemble(...)`. `memset(&result,0,...)` + `AnofoxError error{}` before the call. On `!success`: map `INVALID_MODEL`/`INVALID_INPUT` → `throw InvalidInputException(error.message)` (so unknown-member and blocked-member errors reach SQL), else skip/empty. Emit yhat_lower/yhat_upper as NULL (FFI returns null bound pointers). Mirror the type-preservation + row emission from `_ts_forecast_scalar`.

    **BLOCKING:** Add `src/table_functions/ts_forecast_ensemble_native.cpp` to `CMakeLists.txt` `EXTENSION_SOURCES` immediately after line 180 (`ts_forecast_var_native.cpp`), with a `# Phase 5: ENS-02` comment. Add `#include "ts_forecast_ensemble_native.hpp"  // Phase 5: ENS-02` after extension.cpp line 6, and `RegisterTsForecastEnsembleNativeFunction(loader);  // Phase 5: ENS-02` after line 178.

    Add the `ts_forecast_ensemble_by` macro to ts_macros.cpp mirroring `ts_forecast_by` (line 575): fixed params `{"source","group_col","date_col","target_col","members","horizon","frequency", nullptr}`, named defaults `{{"combination_method","''"},{"seasonal_period","0"},{nullptr,nullptr}}`, body `SELECT group_col, forecast_step, ds, yhat, yhat_lower, yhat_upper, model_name FROM ( SELECT group_col, unnest(_ts_forecast_ensemble_native(LIST(date_col ORDER BY date_col), LIST(target_col::DOUBLE ORDER BY date_col), members, horizon, frequency, combination_method, seasonal_period), recursive := true) FROM query_table(source::VARCHAR) GROUP BY group_col )`. (Per-series, NOT panel — `query_table` is a bare source string here, matching `ts_forecast_by`; the panel subselect gotcha does not apply.)

    Build the extension (`make rust` for the Rust libs if not already current, then the C++/ninja build the project uses — mirror how Phase 4 built: ninja release). Then create `examples/forecasting/ensemble_explicit_tracer.sql`:
      - `LOAD anofox_forecast;`
      - Build a clean 60-obs linearly-trended series (id=1, ds = DATE + i days, y = 10.0 + i*0.5) — same series shape as autoensemble.sql so Auto* selection is deterministic.
      - Section 1 (DoD): `ens_result` = `ts_forecast_ensemble_by('ae_test', id, ds, y, ['AutoARIMA','AutoETS','Naive'], 5, '1d', combination_method := 'mean', seasonal_period := 0)`; three member tables via `ts_forecast_by(... 'AutoARIMA'/'AutoETS'/'Naive' ...)`; `m_manual` = `(y_arima + y_ets + y_naive)/3.0`; a cross-check select showing `diff` and `abs(diff) < 1e-6 AS match`; and an assertion `SELECT count(*) AS mismatch_count FROM ens_result e JOIN m_manual m USING (forecast_step) WHERE abs(e.yhat - m.manual_mean) >= 1e-6;` which MUST return 0.
      - Assert `yhat_lower`/`yhat_upper` are NULL in the ensemble output.
    Run the example against the BUILT extension and confirm mismatch_count = 0 and NULL intervals. (Use `Naive` as the third member — deterministic, always-valid baseline — so the tracer cross-check is exact without relying on Theta selection nuances; Plan 05-02 broadens members.)
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && grep -c 'ts_forecast_ensemble_native.cpp' CMakeLists.txt && grep -c 'ts_forecast_ensemble_by' src/macros/ts_macros.cpp && grep -c 'RegisterTsForecastEnsembleNativeFunction' src/anofox_forecast_extension.cpp && ./build/release/duckdb -unsigned < examples/forecasting/ensemble_explicit_tracer.sql 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `CMakeLists.txt` contains `ts_forecast_ensemble_native.cpp` (BLOCKING wiring present).
    - `src/anofox_forecast_extension.cpp` contains `RegisterTsForecastEnsembleNativeFunction` (>= 1, in the Register* block) and the `#include "ts_forecast_ensemble_native.hpp"`.
    - `src/macros/ts_macros.cpp` contains `ts_forecast_ensemble_by` (>= 1).
    - Extension builds clean (no errors) and loads.
    - `examples/forecasting/ensemble_explicit_tracer.sql` runs clean against the built extension.
    - The assertion query `mismatch_count` returns exactly 0 — Mean ensemble yhat == arithmetic mean of AutoARIMA+AutoETS+Naive independent forecasts within 1e-6.
    - Ensemble `yhat_lower` and `yhat_upper` are NULL in the output.
  </acceptance_criteria>
  <done>ts_forecast_ensemble_by(members := ['AutoARIMA','AutoETS','Naive'], combination_method := 'mean', ...) works end-to-end against the built extension; the Mean cross-check passes with 0 mismatches and NULL intervals; new .cpp is in CMakeLists and registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL user → C++ table/scalar function | User supplies `members VARCHAR[]`, `combination_method`, `seasonal_period` — untrusted input crosses here. |
| C++ → Rust FFI | `members_buf` (null-delimited) + `members_buf_len` + `members_count` cross the ABI; Rust dereferences raw pointers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `members_buf` slice recovery in `anofox_ts_forecast_ensemble` | medium | mitigate | Bound the slice strictly by the explicit `members_buf_len` param (Task 2); never scan past it. No NUL-scan-for-length that could over-read. |
| T-05-02 | Denial of Service | Member count / list validation | low | mitigate | `< 2` members rejected in BOTH C++ Bind (InvalidInputException) and Rust core (InvalidParameter) — defense in depth (Tasks 1 & 3). NULL member list guarded in C++. |
| T-05-03 | Denial of Service | Panic across FFI on malformed member / fit failure | low | mitigate | `catch_unwind(AssertUnwindSafe(...))` wraps the whole core call; panics become `ErrorCode::Panic` → false, not a crash (Task 2, mirrors `anofox_ts_forecast`). |
| T-05-04 | Elevation of Privilege | Member name as code path | low | accept | Member names are validated against the closed 36-name `ModelType` whitelist via `from_str` before any model code runs; unknown/blocked → InvalidParameter. No injection vector; rationale: closed enum, no dynamic dispatch on raw string. |

No high-severity threats (ASVS L1 gate: block-on-high — none present). No new packages (no supply-chain `T-05-SC` needed; stays on anofox-forecast 0.15.3).
</threat_model>

<verification>
- `cargo build -p anofox-fcst-core` and `cargo build -p anofox-fcst-ffi` clean.
- `make header` regenerates the C header with `anofox_ts_forecast_ensemble` + `members_buf_len`.
- Extension builds clean (ninja release) with the new `.cpp` in EXTENSION_SOURCES.
- `examples/forecasting/ensemble_explicit_tracer.sql` runs against the built extension: Mean cross-check `mismatch_count = 0`, NULL intervals.
</verification>

<success_criteria>
- ENS-02 tracer: a 2+ member explicit ensemble with `combination_method := 'mean'` returns a per-series blended forecast, and the blend equals the arithmetic mean of the members' independent `ts_forecast_by` forecasts within 1e-6 (verified example).
- `build_forecaster` is a compiler-exhaustive 36-variant match; the 10 blocked models return `InvalidParameter` naming the member.
- New `.cpp` is in CMakeLists AND registered; macro exists; FFI export present in the regenerated header.
</success_criteria>

<output>
Create `.planning/phases/05-explicit-member-ensemble/05-01-SUMMARY.md` when done.
</output>
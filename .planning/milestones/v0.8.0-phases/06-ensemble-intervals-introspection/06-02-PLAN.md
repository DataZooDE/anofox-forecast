---
phase: 06-ensemble-intervals-introspection
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - examples/forecasting/ensemble_intervals.sql
  - examples/forecasting/ensemble_inspect.sql
  - docs/api/11-conformal-prediction.md
  - docs/api/07-forecasting.md
  - docs/reference/models/ensemble/ensemble_inspect.md
autonomous: true
requirements: [EPI-01, INSP-01]
estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A runnable example produces conformal lower/upper bounds per horizon step on an AutoEnsemble forecast via ts_cv_forecast_by('AutoEnsemble') → ts_conformal_calibrate → apply, with lower<=point<=upper (EPI-01)"
    - "A runnable example produces conformal bounds on an explicit-member ensemble via a manual per-fold loop over ts_cv_folds_by output, with lower<=point<=upper (EPI-01)"
    - "The explicit-member CV-native limitation (ts_forecast_ensemble_by cannot flow through ts_cv_forecast_by; ts_cv_forecast_by ignores top_k/combination_method for AutoEnsemble) is documented in the example + docs (EPI-01)"
    - "The full INSP-01 DoD example asserts explicit Mean weights==1/k, WeightedMSE weights>0 sum to 1, AutoEnsemble Mean weight=1/k score>0, AutoEnsemble WeightedMSE weight NULL score>0 (INSP-01)"
  artifacts:
    - "examples/forecasting/ensemble_intervals.sql — EPI-01 DoD (AutoEnsemble clean path + explicit-member manual-fold, both verified)"
    - "examples/forecasting/ensemble_inspect.sql — INSP-01 full DoD (4 sections)"
    - "docs/api/11-conformal-prediction.md — ensemble conformal section (EPI-01)"
    - "docs/api/07-forecasting.md — ts_ensemble_inspect_by + ts_auto_ensemble_inspect_by entries (INSP-01)"
    - "docs/reference/models/ensemble/ensemble_inspect.md — introspection reference doc (INSP-01)"
  key_links:
    - "EPI-01 uses ONLY existing conformal machinery (ts_cv_folds_by, ts_cv_forecast_by, ts_conformal_calibrate, ts_conformal_apply_by) — no new source code"
    - "Every SQL snippet in both examples and all docs is run through the built extension before commit (PR #230 rule)"
    - "INSP-01 examples depend on the 06-01 macros (ts_ensemble_inspect_by / ts_auto_ensemble_inspect_by) being registered in the built extension"
---

<objective>
Deliver the EPI-01 requirement (ensemble prediction intervals through the existing conformal path — ZERO new source code) and complete the INSP-01 DoD (full introspection example + docs), all verified against the built extension.

EPI-01: a runnable `examples/forecasting/ensemble_intervals.sql` with two sections — (1) AutoEnsemble conformal via the clean `ts_cv_forecast_by('AutoEnsemble')` → `ts_conformal_calibrate` → apply pipeline; (2) explicit-member ensemble conformal via the manual per-fold workaround (since `ts_forecast_ensemble_by` is a ScalarFunction and cannot flow through `ts_cv_forecast_by`). Both produce non-degenerate lower<=point<=upper bounds per horizon step. The CV-native limitations are documented, not hidden.

INSP-01 DoD: a full `examples/forecasting/ensemble_inspect.sql` (4 sections) + a reference doc + API entries, closing the introspection surface wired in 06-01.

Purpose: satisfy the milestone DoD ("EPI-01 routes through the existing conformal path — reuse, do not build new interval machinery") and complete INSP-01 with verified, documented examples.
Output: 2 example SQL files, 3 docs files (2 API entries + 1 new reference doc), all snippets verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ensemble-intervals-introspection/06-RESEARCH.md
@.planning/phases/06-ensemble-intervals-introspection/06-PATTERNS.md
@.planning/phases/06-ensemble-intervals-introspection/06-01-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan produces NO new source symbols. It delivers examples + docs only:

- `examples/forecasting/ensemble_intervals.sql` — EPI-01 DoD (new)
- `examples/forecasting/ensemble_inspect.sql` — INSP-01 full DoD (new)
- `docs/api/11-conformal-prediction.md` — ensemble conformal section appended (modified)
- `docs/api/07-forecasting.md` — ts_ensemble_inspect_by + ts_auto_ensemble_inspect_by entries appended (modified)
- `docs/reference/models/ensemble/ensemble_inspect.md` — new introspection reference doc (new)

EPI-01 reuses the existing conformal SQL surface (`ts_cv_folds_by`, `ts_cv_forecast_by`, `ts_conformal_calibrate`, `ts_conformal_apply_by`) with no code changes. The INSP-01 examples/docs exercise the macros delivered by plan 06-01.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EPI-01 example — ensemble_intervals.sql (AutoEnsemble clean + explicit-member manual fold)</name>
  <files>examples/forecasting/ensemble_intervals.sql</files>
  <read_first>
    - 06-RESEARCH.md lines 88–229 (CRUX A: EPI-01 recipes — AutoEnsemble clean path SQL + explicit-member manual-fold SQL, both VERIFIED; the top_k/method limitation and its documentation requirement)
    - 06-RESEARCH.md lines 820–857 (EPI-01 AutoEnsemble runnable pattern, verified)
    - 06-PATTERNS.md lines 33–137 (example banner/section structure + both EPI-01 recipe blocks)
    - src/macros/ts_macros.cpp lines 1618–1671 (ts_conformal_calibrate returns conformity_score; ts_conformal_apply_by signature)
    - examples/forecasting/ensemble_explicit.sql (banner/.print section structure to mirror)
  </read_first>
  <action>
    Create examples/forecasting/ensemble_intervals.sql as a single file with a header banner (Phase 6, EPI-01, run command `./build/release/duckdb -unsigned < examples/forecasting/ensemble_intervals.sql`, LOAD anofox_forecast) and two `.print`-delimited sections.

    Header block MUST document the two limitations (do not hide them):
    - ts_cv_forecast_by ignores top_k/combination_method params for AutoEnsemble (VERIFIED ts_cv_forecast_native.cpp:380-388) — it always backtests with top_k=3, Mean. The example therefore uses default AutoEnsemble for BOTH backtest and final forecast so calibration matches the forecast model.
    - ts_forecast_ensemble_by is a ScalarFunction and cannot flow through ts_cv_forecast_by; the explicit-member section uses a manual per-fold loop.

    Section 1 — AutoEnsemble conformal (clean path): build a synthetic multi-series dataset (range()-based, ~60 obs/series, trend + noise). Then: ts_cv_folds_by('series', id, ds, y, 3, 5, MAP{}) → folds; ts_cv_forecast_by('folds', id, ds, y, 'AutoEnsemble', MAP{}) → bt; ts_conformal_calibrate('bt', y, yhat, {alpha: 0.1}) → calib (yields conformity_score); ts_forecast_by('series', id, ds, y, 'AutoEnsemble', 5, '1d') → fcst; then emit lower/upper via `f.yhat - c.conformity_score AS yhat_lower, f.yhat + c.conformity_score AS yhat_upper` (CROSS JOIN calib c) OR ts_conformal_apply_by — pick ts_conformal_apply_by if the column names line up, else the explicit CROSS JOIN (both verified in RESEARCH). Materialize the joined result into a table, then the DoD assertion: `SELECT count(*) AS bad_rows FROM <intervals> WHERE yhat_lower > yhat OR yhat > yhat_upper` MUST return 0.

    Section 2 — explicit-member conformal (manual fold workaround): ts_cv_folds_by('series', id, ds, y, 3, 5, MAP{}) → exp_folds. Build exp_bt via the manual-fold CTE from RESEARCH lines 169–227 / PATTERNS lines 100–137: train_data = split='train', test_data = split='test' with ROW_NUMBER step, ens_forecasts = per (id, fold_id) call `unnest(_ts_forecast_ensemble_native(LIST(ds ORDER BY ds), LIST(y::DOUBLE ORDER BY ds), ['AutoARIMA','AutoETS','Theta'], <horizon = max test step>, '1d', '', 0), recursive := true)` GROUP BY id, fold_id, then JOIN forecast to test actuals on (id, fold_id, step==forecast_step). Then ts_conformal_calibrate('exp_bt', y, yhat, {alpha: 0.1}) → exp_calib; ts_forecast_ensemble_by('series', id, ds, y, ['AutoARIMA','AutoETS','Theta'], 5, '1d', combination_method := '') → exp_fcst; emit yhat_lower/yhat_upper via CROSS JOIN exp_calib. Same DoD assertion: bad_rows == 0.

    Run the full file against the built extension (PR #230). Confirm both bad_rows counts are 0 and intervals are non-degenerate (upper > lower where conformity_score > 0).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && ./build/release/duckdb -unsigned < examples/forecasting/ensemble_intervals.sql 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ensemble_intervals.sql exists and contains both `ts_cv_forecast_by` (Section 1) and `_ts_forecast_ensemble_native` (Section 2 manual fold)
    - ensemble_intervals.sql contains `ts_conformal_calibrate` and produces yhat_lower/yhat_upper columns
    - ensemble_intervals.sql header documents the ts_cv_forecast_by top_k/method limitation AND the ScalarFunction manual-fold limitation
    - EPI-01 example: lower <= point <= upper per step — both sections' bad_rows assertion returns 0 against the built extension
    - No new source files created (EPI-01 is example-only)
  </acceptance_criteria>
  <done>EPI-01 example runs clean against the built extension: both AutoEnsemble (clean) and explicit-member (manual-fold) sections produce non-degenerate lower<=point<=upper conformal intervals, with both limitations documented in the file header.</done>
</task>

<task type="auto">
  <name>Task 2: INSP-01 full DoD example — ensemble_inspect.sql (4 sections)</name>
  <files>examples/forecasting/ensemble_inspect.sql</files>
  <read_first>
    - 06-RESEARCH.md lines 600–652 (CRUX C DoD assertion SQL — 4 assertions, use exactly)
    - 06-RESEARCH.md lines 859–882 (INSP-01 runnable pattern)
    - 06-PATTERNS.md lines 581–601 (section map + DoD assertion pattern)
    - examples/forecasting/ensemble_explicit.sql (4-section banner structure + expected-error-at-end idiom)
    - 06-01-PLAN.md (macro signatures: ts_ensemble_inspect_by defaults combination_method/seasonal_period; ts_auto_ensemble_inspect_by defaults top_k/combination_method/seasonal_period + rank column)
  </read_first>
  <action>
    Create examples/forecasting/ensemble_inspect.sql mirroring the ensemble_explicit.sql banner structure (Phase 6, INSP-01, LOAD, .print section banners). Build a synthetic multi-series dataset (range()-based, trend + noise, ~60 obs/series). Four sections, each with the exact DoD assertion from RESEARCH CRUX C:

    Section 1 — explicit-member Mean: ts_ensemble_inspect_by('series', id, ds, y, ['AutoARIMA','AutoETS','Theta'], combination_method := 'mean', seasonal_period := 0). Assert `count(*) FILTER (WHERE abs(weight - 1.0/3.0) > 1e-10)` == 0 (equal weights) AND `count(*) FILTER (WHERE abs(sum_w - 1.0) > 1e-10)` == 0 over `SUM(weight) OVER (PARTITION BY id)` (sum to 1).

    Section 2 — explicit-member WeightedMSE: same members, combination_method := 'weighted_mse'. Assert `count(*) FILTER (WHERE weight < 0)` == 0 (no negatives) AND `count(*) FILTER (WHERE abs(sum_w - 1.0) > 1e-6)` == 0 (sum to 1).

    Section 3 — AutoEnsemble Mean: ts_auto_ensemble_inspect_by('series', id, ds, y, top_k := 3, combination_method := 'mean', seasonal_period := 0). Assert `count(*) FILTER (WHERE weight IS NULL)` == 0 (Mean gives non-NULL weight) AND `count(*) FILTER (WHERE abs(weight - 1.0/k) > 1e-10)` == 0 where k = per-group member count AND `count(*) FILTER (WHERE score <= 0 OR score IS NULL)` == 0 AND rank IN (1..k).

    Section 4 — AutoEnsemble default (WeightedMSE): ts_auto_ensemble_inspect_by('series', id, ds, y, top_k := 3, combination_method := '', seasonal_period := 0). Assert `count(*) FILTER (WHERE weight IS NOT NULL)` == 0 (weight NULL for non-Mean — documented crate 0.15.3 limitation) AND `count(*) FILTER (WHERE score <= 0 OR score IS NULL)` == 0.

    Add a short comment block documenting the AutoEnsemble weight-NULL crate limitation (inner-ensemble combination weights are not accessible via the crate 0.15.3 public API for WeightedMSE/InverseAIC/Stacking/HorizonAdaptive). Run the full file against the built extension (PR #230); confirm all four sections report 0 failures.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && ./build/release/duckdb -unsigned < examples/forecasting/ensemble_inspect.sql 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ensemble_inspect.sql exists with 4 sections calling ts_ensemble_inspect_by (Sections 1–2) and ts_auto_ensemble_inspect_by (Sections 3–4)
    - example: explicit Mean weights all == 1.0/k within 1e-10 and sum(weight)=1 (Section 1, 0 failures)
    - example: WeightedMSE weights >= 0 and sum(weight)=1 per group within 1e-6 (Section 2, 0 failures)
    - example: AutoEnsemble Mean weight = 1/k non-NULL, score>0, rank IN (1..k) (Section 3, 0 failures)
    - example: AutoEnsemble default WeightedMSE weight IS NULL, score>0 (Section 4, 0 failures)
    - ensemble_inspect.sql documents the AutoEnsemble weight-NULL crate 0.15.3 limitation
  </acceptance_criteria>
  <done>The INSP-01 DoD example runs clean against the built extension: all four sections assert 0 failures, proving weight consistency per combination method and the documented AutoEnsemble weight-NULL limitation.</done>
</task>

<task type="auto">
  <name>Task 3: Docs — conformal ensemble section + 2 API entries + introspection reference doc</name>
  <files>docs/api/11-conformal-prediction.md, docs/api/07-forecasting.md, docs/reference/models/ensemble/ensemble_inspect.md</files>
  <read_first>
    - 06-RESEARCH.md lines 731–738 (docs targets), lines 909–924 (open questions: mention predict_with_intervals as widest-envelope alternative; rank column rationale)
    - 06-PATTERNS.md lines 13–15 (docs analogs: ensemble_explicit.md is the reference-doc analog)
    - docs/reference/models/ensemble/ensemble_explicit.md (structure to mirror for ensemble_inspect.md)
    - docs/api/07-forecasting.md (existing ts_forecast_ensemble_by entry — add the two inspect entries after it, same format)
    - docs/api/11-conformal-prediction.md (existing conformal API structure — append an ensemble section)
  </read_first>
  <action>
    (1) docs/api/11-conformal-prediction.md: append an "Ensemble prediction intervals (EPI-01)" section documenting that the conformal path is model-agnostic and works on ensemble forecasts. Include the AutoEnsemble clean recipe and the explicit-member manual-fold recipe (condensed from the verified example), with a clearly-marked LIMITATIONS note: (a) ts_cv_forecast_by ignores top_k/combination_method for AutoEnsemble (uses top_k=3/Mean); (b) ts_forecast_ensemble_by is a ScalarFunction requiring the manual-fold pattern. Briefly mention Ensemble::predict_with_intervals() as a model-native widest-envelope alternative, noting conformal is the distribution-free, coverage-guaranteed path recommended here.

    (2) docs/api/07-forecasting.md: add two entries after the existing ts_forecast_ensemble_by section, matching that entry's format (signature, parameter table, examples, cross-reference):
    - ts_ensemble_inspect_by(source, group_col, date_col, target_col, members, [combination_method], [seasonal_period]) → (group, member_name, weight, score). Note score is NULL; weight sums to 1 per group for all methods; Mean → equal weights. HorizonAdaptive returns the AVERAGE per-step weights (per-step matrix out of scope).
    - ts_auto_ensemble_inspect_by(source, group_col, date_col, target_col, [top_k], [combination_method], [seasonal_period]) → (group, member_name, weight, score, rank). Note weight = 1/k for Mean, NULL otherwise (crate 0.15.3 limitation); score = in-sample MSE; rank = 1..k ascending by MSE; row count may be < top_k if fewer candidates fit.

    (3) docs/reference/models/ensemble/ensemble_inspect.md: new reference doc mirroring ensemble_explicit.md — introduction, the two functions' full signatures + parameter semantics, output column reference, worked examples for each of the four DoD scenarios, the documented AutoEnsemble weight-NULL limitation (with the upstream-enhancement note), the HorizonAdaptive average-weights note, and the model_count() < top_k behavior.

    Run EVERY SQL snippet in all three docs through the built extension (PR #230 rule) before committing. No eyeballed snippets.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && test -f docs/reference/models/ensemble/ensemble_inspect.md && grep -c "ts_ensemble_inspect_by\|ts_auto_ensemble_inspect_by" docs/api/07-forecasting.md && grep -ci "ensemble\|EPI-01" docs/api/11-conformal-prediction.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/models/ensemble/ensemble_inspect.md exists
    - docs/api/07-forecasting.md contains ts_ensemble_inspect_by AND ts_auto_ensemble_inspect_by
    - docs/api/11-conformal-prediction.md contains an ensemble conformal section (references AutoEnsemble + explicit-member)
    - ensemble_inspect.md documents the AutoEnsemble weight-NULL crate 0.15.3 limitation
    - Every SQL snippet across the three docs was run through the built extension (PR #230 rule)
  </acceptance_criteria>
  <done>All three docs are written and every SQL snippet verified against the built extension; the ensemble conformal path (EPI-01) and both introspection functions (INSP-01) are documented with their limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example/docs SQL → built extension | This plan adds NO new code; SQL exercises existing conformal + 06-01 introspection functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Information Disclosure | example/doc datasets | low | accept | All examples use synthetic in-memory range() series — no real/PII data |
| T-06-07 | Tampering | doc SQL snippets drift from runtime behavior | low | mitigate | PR #230 rule — every snippet run through the built extension before commit |

No new code paths introduced; no high-severity threats. EPI-01 reuses the model-agnostic conformal surface (already threat-modeled in prior phases); INSP-01 runtime surface is threat-modeled in 06-01.
</threat_model>

<verification>
- examples/forecasting/ensemble_intervals.sql runs clean against the built extension — both sections bad_rows == 0
- examples/forecasting/ensemble_inspect.sql runs clean — all four sections 0 failures
- All SQL snippets in the three docs verified against the built extension (PR #230)
- Both limitations (CV-native param drop; ScalarFunction manual-fold; AutoEnsemble weight NULL) documented in examples + docs
</verification>

<success_criteria>
- EPI-01 satisfied: verified example produces conformal lower<=point<=upper per step for both AutoEnsemble and explicit-member surfaces, using only the existing conformal machinery
- INSP-01 DoD complete: 4-section introspection example asserts weight consistency per method + AutoEnsemble weight-NULL limitation; reference doc + API entries written and verified
- Both requirements (EPI-01, INSP-01) fully closed with runnable, verified evidence
</success_criteria>

<output>
Create `.planning/phases/06-ensemble-intervals-introspection/06-02-SUMMARY.md` when done
</output>
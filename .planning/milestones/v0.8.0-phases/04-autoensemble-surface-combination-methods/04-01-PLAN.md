---
phase: 04-autoensemble-surface-combination-methods
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/anofox-fcst-core/src/forecast.rs
  - crates/anofox-fcst-ffi/src/types.rs
  - crates/anofox-fcst-ffi/src/lib.rs
  - src/scalar_functions/ts_forecast_scalar.cpp
  - src/table_functions/ts_forecast_native.cpp
  - examples/forecasting/autoensemble.sql
autonomous: true
requirements: [ENS-01, COMB-01]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "SQL: ts_forecast_by(..., 'AutoEnsemble', h, freq, {top_k:3, combination_method:'mean', seasonal_period:0}) returns one blended forecast row-set per series (ENS-01)."
    - "For a clean 60-obs series where all three Auto* members converge, the AutoEnsemble Mean point forecast equals the arithmetic mean of independent AutoARIMA+AutoETS+AutoTheta forecasts within 1e-6 (COMB-01, DoD cross-check)."
    - "Default combination_method (empty string) resolves to Mean, overriding the crate's WeightedMSE default (per CONTEXT locked decision)."
    - "yhat_lower / yhat_upper are NULL for AutoEnsemble in Phase 4 (point forecasts only)."
    - "Pre-existing GARCH / Kalman / AutoARIMA forecasts are unchanged after the additive-ABI struct extension (no regression)."
  artifacts:
    - crates/anofox-fcst-core/src/forecast.rs
    - crates/anofox-fcst-ffi/src/types.rs
    - crates/anofox-fcst-ffi/src/lib.rs
    - src/scalar_functions/ts_forecast_scalar.cpp
    - src/table_functions/ts_forecast_native.cpp
    - anofox_fcst_ffi.h
    - examples/forecasting/autoensemble.sql
  key_links:
    - "ModelType::AutoEnsemble from_str('AutoEnsemble') -> forecast_auto_ensemble -> AutoEnsemble::with_config -> extract_forecast('AutoEnsemble')"
    - "C++ param keys top_k/combination_method -> FFI ensemble_top_k/ensemble_method -> core ForecastOptions.ensemble_top_k/ensemble_method -> parse_combination_method"
    - "make header (cbindgen) regenerates anofox_fcst_ffi.h so C++ sees ensemble_top_k + ensemble_method"
---

<objective>
Wire the crate's `AutoEnsemble` end-to-end through the established Rust-core -> FFI -> C++ -> SQL-macro path as a single vertical tracer slice, exposing `method := 'AutoEnsemble'` on `ts_forecast_by` with `top_k` and `combination_method` params. This plan proves the ONE happy path — `combination_method='mean'`, `top_k=3` — fully wired and demonstrated by a runnable cross-check example against the built extension.

The additive-ABI struct extension (`ensemble_top_k`, `ensemble_method` on both `ForecastOptions` and `ForecastOptionsExog`) touches every layer atomically — it cannot be half-wired — so all wiring sites live in this one plan. The string parser `parse_combination_method` accepts all six methods (Plan 02 exercises the other five), but this plan only demonstrates Mean.

Purpose: Deliver the AutoEnsemble surface (ENS-01) and the Mean combination plus the DoD internal-consistency cross-check (COMB-01), the most predictable and demonstrable success criterion.
Output: All five source files edited additively, regenerated `anofox_fcst_ffi.h`, and a runnable `examples/forecasting/autoensemble.sql` whose Mean cross-check passes against the built extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autoensemble-surface-combination-methods/04-RESEARCH.md
@.planning/phases/04-autoensemble-surface-combination-methods/04-PATTERNS.md
@.claude/skills/anofox-forecast-models/SKILL.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (executor MUST create each with these EXACT names):

Rust core (`crates/anofox-fcst-core/src/forecast.rs`):
- `ModelType::AutoEnsemble` — new enum variant
- `forecast_auto_ensemble(values, horizon, top_k, method_str, period)` — new helper fn
- `parse_combination_method(s: Option<&str>) -> Result<CombinationMethod>` — new helper fn
- `ForecastOptions.ensemble_top_k: usize` and `ForecastOptions.ensemble_method: Option<String>` — new struct fields (core)
- `ForecastOptionsExog.ensemble_top_k: usize` and `ForecastOptionsExog.ensemble_method: Option<String>` — new struct fields (core)

Rust FFI (`crates/anofox-fcst-ffi/src/types.rs`):
- `ForecastOptions.ensemble_top_k: c_int` and `ForecastOptions.ensemble_method: [c_char; 32]` — new FFI struct fields
- `ForecastOptionsExog.ensemble_top_k: c_int` and `ForecastOptionsExog.ensemble_method: [c_char; 32]` — new FFI struct fields

C++ (`ts_forecast_scalar.cpp`, `ts_forecast_native.cpp`):
- bind-data fields `ensemble_top_k` (int64_t) and `ensemble_method` (string) on both bind-data structs
- new MAP param keys `top_k` and `combination_method`

SQL surface:
- method string `'AutoEnsemble'` on `ts_forecast_by`

Generated:
- `anofox_fcst_ffi.h` regenerated via `make header`

File:
- `examples/forecasting/autoensemble.sql` (NEW)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Wire ModelType::AutoEnsemble + parser + helper through the Rust core layer</name>
  <files>crates/anofox-fcst-core/src/forecast.rs</files>
  <read_first>
crates/anofox-fcst-core/src/forecast.rs — read these exact regions before editing:
  - lines 149-157 (ModelType enum, GARCH/Kalman variants — analog to append after)
  - lines 205-210 (from_str exact-match arms, "GARCH"/"Kalman")
  - lines 264-269 (from_str lowercase fallback block, "kalman" arm)
  - lines 320-325 (name() method, ModelType::Kalman => "Kalman")
  - lines 366-396 (ForecastOptions struct + Default impl, kalman_model field at 373 / 394)
  - lines 500-551 (ForecastOptionsExog struct + Default impl + From<ForecastOptions> impl, kalman_model at 503/525/549)
  - lines 735-752 (forecast() match arm ModelType::Kalman at 739; CI-skip guard at 751 "ModelType::GARCH | ModelType::Kalman => (vec![], vec![])")
  - lines 930-941 (SECOND CI-skip guard at 939, same shape)
  - lines 1096-1103 (SECOND forecast dispatch, default-params path, ModelType::Kalman => forecast_kalman(values, horizon, None) at 1102)
  - lines 2453-2464 (forecast_kalman helper — the exact analog for forecast_auto_ensemble)
  - lines 2733-2739 (calculate_fitted_values match, GARCH|Kalman => vec![] at 2738)
04-PATTERNS.md "Pattern Assignments" for forecast.rs (Steps 1-8) — verbatim code to copy.
  </read_first>
  <action>
Make eight additive edits to forecast.rs, mirroring the Kalman precedent exactly:

1. ModelType enum (after `Kalman,` at ~line 156): add variant `AutoEnsemble` with a doc comment describing it as auto-fitting AutoARIMA/AutoETS/AutoTheta, ranking by in-sample MSE, and combining top-K members using the chosen combination method.

2. from_str exact-match (after `"Kalman" => return Ok(ModelType::Kalman),` at ~line 209): add `"AutoEnsemble" => return Ok(ModelType::AutoEnsemble),`.

3. from_str lowercase fallback (after `"kalman" => Ok(ModelType::Kalman),` at ~line 268): add `"autoensemble" | "auto_ensemble" => Ok(ModelType::AutoEnsemble),`.

4. name() method (after `ModelType::Kalman => "Kalman",` at ~line 324): add `ModelType::AutoEnsemble => "AutoEnsemble",`.

5. ForecastOptions struct (after `pub kalman_model: Option<String>,` at ~line 373): add two fields `pub ensemble_top_k: usize,` and `pub ensemble_method: Option<String>,` with doc comments (top_k 0 means default 3; method None means Mean, the Phase 4 default). Add to its Default impl (after `kalman_model: None,` at ~line 394): `ensemble_top_k: 0,` and `ensemble_method: None,`.

6. ForecastOptionsExog struct (after its `pub kalman_model: Option<String>,` at ~line 503): add the SAME two fields. Add to its Default impl (after `kalman_model: None,` at ~line 525): `ensemble_top_k: 0,` and `ensemble_method: None,`. Add to the `From<ForecastOptions> for ForecastOptionsExog` impl (after `kalman_model: opts.kalman_model,` at ~line 549): `ensemble_top_k: opts.ensemble_top_k,` and `ensemble_method: opts.ensemble_method,`.

7. forecast() dispatch: at the FIRST match (~line 739-743) add after the Kalman arm: `ModelType::AutoEnsemble => forecast_auto_ensemble(&clean_values, options.horizon, if options.ensemble_top_k == 0 { 3 } else { options.ensemble_top_k }, options.ensemble_method.as_deref(), period),`. At the SECOND dispatch (~line 1102, default-params path) add after the Kalman arm: `ModelType::AutoEnsemble => forecast_auto_ensemble(values, horizon, 3, None, period),`. Join AutoEnsemble to BOTH CI-skip guards (~line 751 and ~line 939): change each `ModelType::GARCH | ModelType::Kalman => (vec![], vec![]),` to add `| ModelType::AutoEnsemble` before the fat arrow — AutoEnsemble is point-forecast-only in Phase 4 (intervals deferred to Phase 6/EPI-01). Join AutoEnsemble to the calculate_fitted_values guard (~line 2738): change `ModelType::GARCH | ModelType::Kalman => vec![],` to add `| ModelType::AutoEnsemble` before the arrow.

8. Add two new helper fns after `forecast_kalman` (~line 2464). First `parse_combination_method(s: Option<&str>) -> Result<CombinationMethod>` matching on `s.unwrap_or("").trim().to_lowercase().as_str()`: empty-or-"mean" -> `CombinationMethod::Mean`; "median" -> `Median`; "weighted_mse"/"weightedmse"/"weighted-mse" -> `WeightedMSE`; "inverse_aic"/"inverseaic"/"inverse-aic"/"aic" -> `InverseAIC`; "stacking"/"stack" -> `Stacking { folds: 2 }`; "horizon_adaptive"/"horizonadaptive"/"horizon-adaptive"/"adaptive" -> `HorizonAdaptive`; any other value -> `Err(ForecastError::InvalidParameter { param: "combination_method".into(), value: other.into(), reason: "expected one of: mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive".into() })`. Do NOT accept "custom" (deferred, ENS-F1). Then `forecast_auto_ensemble(values, horizon, top_k, method_str, period)`: call `parse_combination_method(method_str)?`; build `AutoEnsembleConfig { top_k, combination_method, seasonal_period: if period > 1 { Some(period) } else { None } }`; `make_timeseries(values)?`; `AutoEnsemble::with_config(config)`; `model.fit(&ts).map_err(|e| ForecastError::ComputationError(format!("AutoEnsemble fit failed: {}", e)))?`; return `extract_forecast(&model, horizon, "AutoEnsemble")`. Add `use anofox_forecast::models::ensemble::{AutoEnsemble, AutoEnsembleConfig};` and `use anofox_forecast::models::ensemble::model::CombinationMethod;`. If those exact module paths do not resolve, grep the 0.15.3 crate source under ~/.cargo/registry/src for the correct `pub use` path and adjust the imports only (do NOT change behavior).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && cargo build -p anofox-fcst-core 2>&1 | tail -20 && cargo test -p anofox-fcst-core forecast_kalman 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-fcst-core` compiles clean (no errors).
    - `grep -c 'ModelType::AutoEnsemble' crates/anofox-fcst-core/src/forecast.rs` returns >= 4.
    - forecast.rs contains `fn forecast_auto_ensemble(` exactly once.
    - forecast.rs contains `fn parse_combination_method(` exactly once.
    - forecast.rs contains `pub ensemble_top_k: usize` and `pub ensemble_method: Option<String>` (each appears twice — once per core struct).
    - The `From<ForecastOptions> for ForecastOptionsExog` impl copies `ensemble_top_k` and `ensemble_method`.
    - Existing Kalman tests still pass: `cargo test -p anofox-fcst-core forecast_kalman` is green.
  </acceptance_criteria>
  <done>ModelType::AutoEnsemble dispatches to forecast_auto_ensemble via parse_combination_method; the core crate builds and pre-existing Kalman tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend both FFI structs additively, thread fields, regenerate C header</name>
  <files>crates/anofox-fcst-ffi/src/types.rs, crates/anofox-fcst-ffi/src/lib.rs</files>
  <read_first>
crates/anofox-fcst-ffi/src/types.rs — read exact regions:
  - lines 406-414 (ForecastOptions #[repr(C)], last fields garch_p/garch_q/kalman_model at 407/409/413)
  - lines 488-500 (ForecastOptions Default impl zero-init block, "kalman_model: [0; 32]," at 496)
  - lines 567-573 (ForecastOptionsExog #[repr(C)], kalman_model at 572)
  - lines 593-603 (ForecastOptionsExog Default impl zero-init, "kalman_model: [0; 32]," at 599)
crates/anofox-fcst-ffi/src/lib.rs — read exact regions:
  - lines 3452-3476 (anofox_ts_forecast: kalman_model CStr parse at 3453-3457; core_opts literal, "kalman_model," at 3475)
  - lines 3743-3768 (anofox_ts_forecast_with_exog: kalman_model_exog parse at 3744-3748; ForecastOptionsExog literal, "kalman_model: kalman_model_exog," at 3767)
  - lines 4118-4177 (build_core_options: kalman_model parse at 4152; returned ForecastOptions literal ending with "kalman_model,")
04-PATTERNS.md "Pattern Assignments" for types.rs and lib.rs — verbatim code + the "append at END" ABI rule.
04-RESEARCH.md Pitfall 1 (ABI break from middle-insertion) and Pitfall 3 (must update BOTH structs).
  </read_first>
  <action>
types.rs — additive APPEND ONLY. Never reorder existing fields; middle-insertion shifts #[repr(C)] offsets and silently corrupts GARCH/Kalman callers.
1. ForecastOptions #[repr(C)] (after `pub kalman_model: [c_char; 32],` at ~line 413, still inside the struct braces): append `pub ensemble_top_k: c_int,` and `pub ensemble_method: [c_char; 32],` with doc comments (top_k 0 -> default 3; ensemble_method empty -> mean; only consulted when model is AutoEnsemble).
2. ForecastOptions Default impl (after `kalman_model: [0; 32],` at ~line 496): append `ensemble_top_k: 0,` and `ensemble_method: [0; 32],`.
3. ForecastOptionsExog #[repr(C)] (after its `pub kalman_model: [c_char; 32],` at ~line 572): append the SAME two pub fields identically.
4. ForecastOptionsExog Default impl (after `kalman_model: [0; 32],` at ~line 599): append `ensemble_top_k: 0,` and `ensemble_method: [0; 32],`.

lib.rs — thread the fields in all THREE core-options construction sites (each must compile against the extended core ForecastOptions/ForecastOptionsExog from Task 1):
5. anofox_ts_forecast (~line 3457, after the kalman_model CStr block): add an `ensemble_method` parse identical to kalman_model's pattern (`CStr::from_ptr(opts.ensemble_method.as_ptr()).to_str().ok().filter(|s| !s.is_empty()).map(str::to_owned)`). In the core_opts ForecastOptions literal (after `kalman_model,` at ~line 3475) add `ensemble_top_k: opts.ensemble_top_k as usize,` and `ensemble_method,`.
6. anofox_ts_forecast_with_exog (~line 3748, after kalman_model_exog): add `ensemble_method_exog` parse (same pattern). In the ForecastOptionsExog literal (after `kalman_model: kalman_model_exog,` at ~line 3767) add `ensemble_top_k: opts.ensemble_top_k as usize,` and `ensemble_method: ensemble_method_exog,`.
7. build_core_options (~line 4152, after its kalman_model parse): add the same `ensemble_method` parse; in the returned ForecastOptions literal (after `kalman_model,`) add `ensemble_top_k: opts.ensemble_top_k as usize,` and `ensemble_method,`.

8. Regenerate the C header (MANDATORY — C++ cannot see the new fields otherwise): run `make header`. Confirm `anofox_fcst_ffi.h` now contains `ensemble_top_k` and `ensemble_method` on both struct definitions.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && cargo build -p anofox-fcst-ffi 2>&1 | tail -20 && make header 2>&1 | tail -5 && grep -c ensemble_top_k anofox_fcst_ffi.h && grep -c ensemble_method anofox_fcst_ffi.h</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-fcst-ffi` compiles clean.
    - types.rs: `grep -c 'pub ensemble_top_k: c_int' crates/anofox-fcst-ffi/src/types.rs` returns 2 (both structs); `grep -c 'pub ensemble_method: \[c_char; 32\]' crates/anofox-fcst-ffi/src/types.rs` returns 2.
    - After `make header`, `grep -c ensemble_top_k anofox_fcst_ffi.h` returns >= 2 and `grep -c ensemble_method anofox_fcst_ffi.h` returns >= 2.
    - lib.rs: `grep -c 'opts.ensemble_top_k as usize' crates/anofox-fcst-ffi/src/lib.rs` returns 3 (three construction sites).
    - Existing fields kalman_model/garch_p/garch_q remain in their original positions (append-only — no reorder).
  </acceptance_criteria>
  <done>Both FFI structs carry ensemble_top_k + ensemble_method at the END; all three lib.rs construction sites thread them; anofox_fcst_ffi.h is regenerated and exposes the fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add C++ param keys on both dispatch paths, build the extension, and demonstrate the Mean cross-check</name>
  <files>src/scalar_functions/ts_forecast_scalar.cpp, src/table_functions/ts_forecast_native.cpp, examples/forecasting/autoensemble.sql</files>
  <read_first>
src/scalar_functions/ts_forecast_scalar.cpp — read exact regions (5 kalman_model sites):
  - lines 50-55 (TsForecastScalarBindData struct, "string kalman_model = "";" at 53)
  - lines 70-76 (Copy() method, "copy->kalman_model = kalman_model;" at 74)
  - lines 128-165 (ValidateParams valid_keys set at 131; error message string at 162 listing valid params)
  - lines 428-432 (local var decls in TsForecastScalarExecute, "string kalman_model = bind_data.kalman_model;" at 431)
  - lines 445-449 (MAP/STRUCT params parse, "kalman_model = ParseStringParam(params_val, "kalman_model", "");" at 448)
  - lines 481-489 (opts building, kalman_model strncpy guard at 484-488)
src/table_functions/ts_forecast_native.cpp — read exact regions (4 kalman_model sites):
  - lines 50-54 (TsForecastNativeBindData struct, "string kalman_model = "";" at 53)
  - lines 274-309 (ValidateParamKeys valid_keys set at 278; error message at 308)
  - lines 360-364 (Bind params parse, "bind_data->kalman_model = ParseStringFromParams(params, "kalman_model", "");" at 363)
  - lines 660-667 (opts building, kalman_model strncpy guard at 663-666)
04-PATTERNS.md "Pattern Assignments" for both cpp files — the String->c_char array copy pattern (strncpy + explicit NUL).
04-RESEARCH.md section "Verification / Cross-Check Approach (DoD)" and "Validation Architecture" — the exact example SQL structure + the "all three members must converge / top_k=3" caveat.
existing examples/forecasting/*.sql — one existing file for header/comment style (role-match analog).
  </read_first>
  <action>
ts_forecast_scalar.cpp — 5 additive sites, mirroring kalman_model exactly:
1. TsForecastScalarBindData struct (after `string kalman_model = "";` at ~line 53): add `int64_t ensemble_top_k = 0;` and `string ensemble_method = "";`.
2. Copy() method (after `copy->kalman_model = kalman_model;` at ~line 74): add `copy->ensemble_top_k = ensemble_top_k;` and `copy->ensemble_method = ensemble_method;`.
3. ValidateParams valid_keys set (~line 131): add `"top_k", "combination_method"` to the set. Update the error-message string at ~line 162 to append `, top_k, combination_method` to the listed valid parameters.
4. Local var decls in TsForecastScalarExecute (after `string kalman_model = bind_data.kalman_model;` at ~line 431): add `int64_t ensemble_top_k = bind_data.ensemble_top_k;` and `string ensemble_method = bind_data.ensemble_method;`.
5. MAP/STRUCT parse (after `kalman_model = ParseStringParam(params_val, "kalman_model", "");` at ~line 448): add `ensemble_top_k = ParseInt64Param(params_val, "top_k", 0);` and `ensemble_method = ParseStringParam(params_val, "combination_method", "");`.
6. opts building (after the kalman_model strncpy block at ~line 488): add `opts.ensemble_top_k = static_cast<int>(ensemble_top_k);` then a guarded copy `if (!ensemble_method.empty()) { strncpy(opts.ensemble_method, ensemble_method.c_str(), sizeof(opts.ensemble_method) - 1); opts.ensemble_method[sizeof(opts.ensemble_method) - 1] = '\0'; }`.

ts_forecast_native.cpp — 4 additive sites (same field names):
7. TsForecastNativeBindData struct (after `string kalman_model = "";` at ~line 53): add `int64_t ensemble_top_k = 0;` and `string ensemble_method = "";`.
8. ValidateParamKeys valid_keys set (~line 278): add `"top_k", "combination_method"`. Update the error-message string at ~line 308 to append `, top_k, combination_method`.
9. Bind params parse (after `bind_data->kalman_model = ParseStringFromParams(params, "kalman_model", "");` at ~line 363): add `bind_data->ensemble_top_k = ParseInt64FromParams(params, "top_k", 0);` and `bind_data->ensemble_method = ParseStringFromParams(params, "combination_method", "");`.
10. opts building (after the kalman_model strncpy block at ~line 666): add `opts.ensemble_top_k = static_cast<int>(bind_data.ensemble_top_k);` then the guarded strncpy copy for `opts.ensemble_method` from `bind_data.ensemble_method` (same guard shape as scalar site 6).

No macro change: `ts_forecast_by` in src/macros/ts_macros.cpp passes the params MAP through verbatim — do NOT edit it. No new C++ source file — do NOT touch CMakeLists.

11. Build the full extension: run `make rust` (or the project's standard extension build). Fix any C++ compile errors from the new fields before proceeding.

12. Create `examples/forecasting/autoensemble.sql`. Follow 04-RESEARCH.md "Validation Architecture" verbatim structure:
    - Header comment block: state the invariant "Mean == arithmetic mean of members holds ONLY when all three Auto* members converge AND top_k>=3; this example uses a clean 60-obs series and top_k=3 so no member is dropped."
    - LOAD the built extension (match how other examples/forecasting/*.sql load it).
    - Create a clean 60-observation synthetic series: `CREATE OR REPLACE TABLE ae_test AS SELECT i AS id, '2020-01-01'::DATE + INTERVAL (i-1) DAY AS ds, 10.0 + i * 0.5 AS y FROM generate_series(1, 60) t(i);` (single series id — id constant is fine; if the macro needs a real group key, use a constant literal group column).
    - Step 1: AutoEnsemble Mean, `ts_forecast_by('ae_test', id, ds, y, 'AutoEnsemble', 5, '1d', {top_k: 3, combination_method: 'mean', seasonal_period: 0})` into table ae_mean.
    - Step 2: independent AutoARIMA, AutoETS, AutoTheta forecasts (same horizon 5, freq '1d', seasonal_period 0) into ae_arima/ae_ets/ae_theta, each selecting `forecast_step, yhat`.
    - Step 3: manual mean `(y_arima + y_ets + y_theta) / 3.0` joined on forecast_step into ae_manual.
    - Step 4: cross-check SELECT joining ae_mean and ae_manual on forecast_step, computing `abs(ensemble_yhat - manual_mean) < 1e-6 AS match`, ORDER BY forecast_step. Every row must show match=true.
    - Add a final assertion query that RAISES/errors if any row mismatches, so a non-interactive run fails loudly (e.g. a SELECT that would return rows only on mismatch, or use the project's example-assertion convention if one exists).

13. Run the example against the BUILT extension end-to-end and confirm the cross-check passes (all match=true rows). This is the PR #230 rule: run it, do not eyeball.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && make rust 2>&1 | tail -25 && duckdb < examples/forecasting/autoensemble.sql 2>&1 | tail -40</automated>
    <human-check>Open examples/forecasting/autoensemble.sql output: confirm every cross-check row shows match=true and the AutoEnsemble yhat column is finite and non-NULL, while yhat_lower/yhat_upper are NULL.</human-check>
  </verify>
  <acceptance_criteria>
    - `make rust` builds the extension clean (no C++ errors).
    - ts_forecast_scalar.cpp contains `"top_k", "combination_method"` in its valid_keys set and threads `opts.ensemble_top_k` + `opts.ensemble_method`.
    - ts_forecast_native.cpp contains `"top_k", "combination_method"` in its valid_keys set and threads `opts.ensemble_top_k` + `opts.ensemble_method`.
    - `examples/forecasting/autoensemble.sql` exists and runs clean against the built extension.
    - Cross-check: for the 60-obs series, every forecast_step row has `abs(ensemble_yhat - manual_mean) < 1e-6` = true (Mean == arithmetic mean of AutoARIMA+AutoETS+AutoTheta).
    - AutoEnsemble output yhat is finite/non-NULL; yhat_lower and yhat_upper are NULL (point forecasts only in Phase 4).
    - A stale param (e.g. `{foo: 1}`) still raises "Unknown parameter(s)" and now lists top_k, combination_method as valid.
  </acceptance_criteria>
  <done>`ts_forecast_by(..., 'AutoEnsemble', ..., {top_k, combination_method})` runs end-to-end; the runnable example proves Mean combination equals the arithmetic mean of the three independent member forecasts within 1e-6 against the built extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL user -> C++ params MAP | Untrusted `top_k` (int) and `combination_method` (string) enter here from a `ts_forecast_by` call. |
| C++ -> Rust FFI (`#[repr(C)]` structs) | `ensemble_method` crosses as a fixed-size `[c_char; 32]`; `ensemble_top_k` as `c_int`. |
| Rust FFI -> Rust core | `combination_method` string parsed by `parse_combination_method`; unknown strings rejected. |

This is a local, in-process analytics extension — no network, no auth surface. The threat surface is narrow: FFI buffer handling, integer validation, and panic-safety across the catch_unwind boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `ensemble_method` `[c_char; 32]` copy in both cpp files | low | mitigate | `strncpy(..., sizeof(field) - 1)` + explicit NUL terminator, guarded by `if (!ensemble_method.empty())`; no unbounded copy, no overrun (analog: kalman_model). |
| T-04-02 | Denial of Service | `ensemble_top_k` integer bound | low | accept | Crate clamps top-K internally via `top_k.min(candidates.len())`; a large or negative value cannot exceed the 3 candidate families. `ensemble_top_k == 0` maps to default 3. No unbounded allocation. |
| T-04-03 | Denial of Service | `AutoEnsemble::fit` panic / convergence failure across FFI | low | mitigate | FFI boundary already wraps calls in `catch_unwind`; `ConvergenceFailure` propagates as `ComputationError` -> DuckDB `InvalidInputException`. No silent NULL, no crash. |
| T-04-04 | Information Disclosure | Unknown `combination_method` string | low | mitigate | `parse_combination_method` returns `InvalidParameter` with a fixed allow-list message; `custom` (ENS-F1) explicitly rejected. No reflection of raw memory. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies added — stays on already-vendored anofox-forecast 0.15.3; no package-manager install occurs in this phase. |

No high-severity threats. All boundary checks reuse the proven GARCH/Kalman FFI pattern.
</threat_model>

<verification>
## Phase-level checks (Plan 01)
- Rust core + FFI crates build clean; pre-existing GARCH/Kalman/AutoARIMA behavior unchanged (additive-ABI, no field reorder).
- `anofox_fcst_ffi.h` regenerated by `make header` and exposes `ensemble_top_k` + `ensemble_method` on both structs.
- Full extension builds via `make rust`.
- `examples/forecasting/autoensemble.sql` runs clean against the built extension and its Mean cross-check passes (every row match=true, tolerance 1e-6).
- `yhat_lower`/`yhat_upper` are NULL for AutoEnsemble.
</verification>

<success_criteria>
- ENS-01 (partial — Mean path): a user runs an AutoEnsemble forecast per series via `ts_forecast_by(..., 'AutoEnsemble', h, freq, {top_k, combination_method, seasonal_period})` and gets one blended forecast row-set per series.
- COMB-01 (Mean): `combination_method='mean'` (and the empty-string default) produce a point forecast equal to the arithmetic mean of the three independent member forecasts within 1e-6, demonstrated by a runnable example against the built extension.
- No regression to existing methods (additive ABI verified).
</success_criteria>

<output>
Create `.planning/phases/04-autoensemble-surface-combination-methods/04-01-SUMMARY.md` when done.
</output>


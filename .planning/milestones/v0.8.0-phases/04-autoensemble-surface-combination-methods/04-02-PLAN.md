---
phase: 04-autoensemble-surface-combination-methods
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - examples/forecasting/autoensemble.sql
  - docs/reference/models/ensemble/autoensemble.md
  - docs/api/07-forecasting.md
autonomous: true
requirements: [COMB-01, COMB-02, COMB-03, COMB-04]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "All six combination methods (mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive) produce a non-NULL finite yhat per forecast step through ts_forecast_by 'AutoEnsemble' against the built extension (COMB-01..04)."
    - "Mean and Median produce visibly different point forecasts on a skewed series (COMB-01 demonstrability)."
    - "WeightedMSE and InverseAIC apply error/information-weighted blending (COMB-02); Stacking and HorizonAdaptive return valid forecasts (COMB-03, COMB-04)."
    - "docs document the 'AutoEnsemble' method string, top_k (default 3), combination_method (default mean) with all six accepted strings + aliases, and the Phase-4 NULL-interval note."
  artifacts:
    - examples/forecasting/autoensemble.sql
    - docs/reference/models/ensemble/autoensemble.md
    - docs/api/07-forecasting.md
  key_links:
    - "combination_method string -> parse_combination_method (Plan 01) -> CombinationMethod enum -> Ensemble blend"
    - "docs SQL snippets -> run through built extension (PR #230 rule)"
---

<objective>
Expand the tracer slice from Plan 01 (which proved the Mean path end-to-end) to demonstrate the remaining five combination methods (Median, WeightedMSE, InverseAIC, Stacking, HorizonAdaptive) and the Mean-vs-Median demonstrability requirement, then document the AutoEnsemble surface. The wiring already exists — `parse_combination_method` in Plan 01 accepts all six methods — so this plan adds no source-code wiring; it exercises and documents the surface.

Purpose: Close COMB-01 (Median demonstrability), COMB-02 (WeightedMSE + InverseAIC), COMB-03 (Stacking), COMB-04 (HorizonAdaptive), and satisfy the docs half of the DoD (every doc SQL snippet run through the built extension, PR #230 rule).
Output: an expanded `examples/forecasting/autoensemble.sql` six-method smoke test + Mean-vs-Median demonstration, a new `docs/reference/models/ensemble/autoensemble.md`, and an updated `docs/api/07-forecasting.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autoensemble-surface-combination-methods/04-RESEARCH.md
@.planning/phases/04-autoensemble-surface-combination-methods/04-PATTERNS.md
@.claude/skills/anofox-forecast-models/SKILL.md
</context>

<artifacts_this_phase_produces>
NEW / UPDATED artifacts introduced by this plan:
- `examples/forecasting/autoensemble.sql` — EXPANDED with a six-method smoke test (one block per combination_method: mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive) and a Mean-vs-Median demonstrability block on a skewed series.
- `docs/reference/models/ensemble/autoensemble.md` — NEW model reference doc for the 'AutoEnsemble' method string.
- `docs/api/07-forecasting.md` — UPDATED to list 'AutoEnsemble' + the top_k / combination_method params.

No source-code / FFI / C++ changes in this plan — the wiring landed in Plan 01. `parse_combination_method` (Plan 01) already accepts all six method strings and their aliases.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Exercise all six combination methods + Mean-vs-Median demonstrability against the built extension</name>
  <files>examples/forecasting/autoensemble.sql</files>
  <read_first>
examples/forecasting/autoensemble.sql — the file created in Plan 01 (Mean cross-check + 60-obs series). Read it fully; you will APPEND to it, not rewrite the cross-check.
04-RESEARCH.md sections "Verification / Cross-Check Approach (DoD)" (the six-method smoke test + "Mean vs Median demonstrability" guidance) and "Validation Architecture" ("Six-method smoke test: one SQL block per COMB variant; each must return a non-NULL, finite yhat for each forecast step").
04-RESEARCH.md "CombinationMethod Enum" + "String→enum accepted values table" — the exact six method strings: mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive.
  </read_first>
  <action>
Append to `examples/forecasting/autoensemble.sql` (keep the Plan-01 Mean cross-check intact at the top):

1. Six-method smoke test. For EACH of the six combination_method strings (`'mean'`, `'median'`, `'weighted_mse'`, `'inverse_aic'`, `'stacking'`, `'horizon_adaptive'`) run `ts_forecast_by('ae_test', id, ds, y, 'AutoEnsemble', 5, '1d', {top_k: 3, combination_method: '<method>', seasonal_period: 0})` and assert every forecast_step yhat is non-NULL and finite. Prefer a single UNION-ALL query that tags each row with its method label and a boolean `ok := yhat IS NOT NULL AND isfinite(yhat)`, then a final assertion query that returns rows ONLY on failure (so a non-interactive `duckdb <` run fails loudly). Use the project's existing example-assertion convention if one is present in other examples/forecasting/*.sql; otherwise a `SELECT ... WHERE NOT ok` that must return zero rows.

2. Mean-vs-Median demonstrability (COMB-01). Create a SKEWED series where member forecasts diverge (e.g. exponential growth with a few large outlier spikes, ~60 obs) into table `ae_skew`. Run AutoEnsemble with `combination_method='mean'` and again with `combination_method='median'` (top_k=3, seasonal_period=0). Produce a comparison query joining the two on forecast_step showing `mean_yhat`, `median_yhat`, and `abs(mean_yhat - median_yhat) AS delta`. Add an assertion that at least one forecast_step has `delta > 1e-6` (they must be visibly different — do not merely claim it).

3. Add brief comments labeling each block with the COMB requirement it demonstrates (COMB-01 mean/median, COMB-02 weighted_mse/inverse_aic, COMB-03 stacking, COMB-04 horizon_adaptive).

Run the full expanded example against the BUILT extension (PR #230 rule: run it, do not eyeball). All assertions must pass.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && duckdb < examples/forecasting/autoensemble.sql 2>&1 | tail -50</automated>
    <human-check>Confirm the six-method smoke test shows a non-NULL finite yhat for every method, and the Mean-vs-Median block shows at least one forecast_step where mean_yhat != median_yhat.</human-check>
  </verify>
  <acceptance_criteria>
    - `examples/forecasting/autoensemble.sql` runs clean end-to-end against the built extension (exit 0, no error).
    - All six combination_method strings (mean, median, weighted_mse, inverse_aic, stacking, horizon_adaptive) return a non-NULL finite yhat for every forecast step (COMB-01..04).
    - The Mean-vs-Median block shows `delta > 1e-6` for at least one forecast_step on the skewed series (COMB-01 demonstrability).
    - The Plan-01 Mean cross-check block remains present and still passes.
    - Any assertion query returns zero rows on success and would return rows (fail loudly) on any NULL/non-finite yhat.
  </acceptance_criteria>
  <done>All six combination methods are demonstrated to produce valid per-series forecasts against the built extension, and Mean vs Median is shown to differ on a skewed series.</done>
</task>

<task type="auto">
  <name>Task 2: Document the AutoEnsemble surface and verify every doc snippet against the built extension</name>
  <files>docs/reference/models/ensemble/autoensemble.md, docs/api/07-forecasting.md</files>
  <read_first>
docs/api/07-forecasting.md — read fully; find where existing methods (GARCH, Kalman, AutoARIMA, AutoETS, AutoTheta) and their params are documented, to match structure and placement.
An existing model reference under docs/reference/models/ (e.g. docs/reference/models/state-space/ or docs/reference/models/classical/) — read one file for the exact doc template/format (frontmatter, section headings, example blocks).
04-RESEARCH.md "String→enum accepted values table" (the six methods + aliases) and "Prediction Intervals Behaviour" (yhat_lower/yhat_upper NULL in Phase 4).
04-CONTEXT.md decisions (default combination_method = Mean; default top_k = 3; seasonal_period reused; Stacking folds not exposed; candidate families fixed to ARIMA/ETS/Theta).
Project memory rule: every doc SQL snippet must run through the built extension (PR #230). Note ts_forecast_by is now the delivery vehicle; do NOT reference removed functions (e.g. ts_backtest_auto_by).
  </read_first>
  <action>
1. Create `docs/reference/models/ensemble/autoensemble.md` following the existing model-reference doc format. Document:
   - Method string: `'AutoEnsemble'` on `ts_forecast_by`.
   - What it does: auto-fits AutoARIMA / AutoETS / AutoTheta, ranks by in-sample MSE, combines the top-K.
   - Params: `top_k` (default 3), `combination_method` (default `'mean'`), and the shared `seasonal_period` (0 -> non-seasonal; p>1 -> seasonal sub-models).
   - All six accepted `combination_method` strings with their aliases: mean; median; weighted_mse (weightedmse, weighted-mse); inverse_aic (inverseaic, inverse-aic, aic); stacking (stack); horizon_adaptive (horizonadaptive, horizon-adaptive, adaptive). Note `'custom'` is NOT accepted (deferred).
   - Phase-4 limitation note: `yhat_lower` / `yhat_upper` are NULL (point forecasts only; conformal ensemble intervals are a later phase).
   - Note that candidate families (ARIMA/ETS/Theta) are fixed and Stacking `folds` is not exposed in v1.
   - At least one runnable `ts_forecast_by(..., 'AutoEnsemble', ...)` SQL example, taken from the verified example file so it is known-good.
2. Update `docs/api/07-forecasting.md` to add `'AutoEnsemble'` to the method list/table and document the `top_k` + `combination_method` params in the same style as the existing GARCH/Kalman param docs. Link to the new reference doc.
3. Verify EVERY new/edited SQL snippet in both docs by running it through the built extension (PR #230 rule). Extract the snippets and run them (e.g. pipe each into `duckdb`); confirm each returns without error and produces the described output. Fix any snippet that references a removed/renamed function or wrong param key.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-forecast && grep -c "AutoEnsemble" docs/reference/models/ensemble/autoensemble.md && grep -c "AutoEnsemble" docs/api/07-forecasting.md && grep -Eic "combination_method|top_k" docs/reference/models/ensemble/autoensemble.md</automated>
    <human-check>Manually run each SQL snippet from docs/reference/models/ensemble/autoensemble.md and the AutoEnsemble snippet(s) in docs/api/07-forecasting.md through the built extension; confirm each executes clean and matches the documented result. Confirm no snippet references a removed function (e.g. ts_backtest_auto_by).</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/reference/models/ensemble/autoensemble.md` exists and documents the 'AutoEnsemble' method string, top_k (default 3), combination_method (default mean) with all six strings + aliases, the fixed ARIMA/ETS/Theta families, and the yhat_lower/yhat_upper NULL note.
    - `docs/api/07-forecasting.md` lists 'AutoEnsemble' and documents top_k + combination_method.
    - Every SQL snippet in both docs runs clean against the built extension (no removed/renamed function references, no invalid param keys).
    - `grep -c AutoEnsemble docs/reference/models/ensemble/autoensemble.md` >= 3.
  </acceptance_criteria>
  <done>The AutoEnsemble surface is documented in both the model reference and the forecasting API doc, and every doc SQL snippet is verified against the built extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL user -> ts_forecast_by params | Same boundary as Plan 01; this plan only exercises and documents it, adds no new input path. |

This plan writes example SQL and documentation only — no new code, no new FFI surface. The threat surface is inherited from Plan 01 and unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure | Doc SQL snippets | low | mitigate | Every snippet is run through the built extension before publishing (PR #230 rule); no stale/removed-function references shipped. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No dependencies added; docs + example SQL only. |

No high-severity threats. No new trust boundary is introduced by this plan.
</threat_model>

<verification>
## Phase-level checks (Plan 02)
- `examples/forecasting/autoensemble.sql` (expanded) runs clean against the built extension; all six methods return finite non-NULL yhat; Mean vs Median differ on the skewed series.
- `docs/reference/models/ensemble/autoensemble.md` created; `docs/api/07-forecasting.md` updated; every doc SQL snippet verified against the built extension.
</verification>

<success_criteria>
- COMB-01: Median demonstrated distinct from Mean on a skewed series.
- COMB-02: WeightedMSE and InverseAIC produce valid per-series forecasts.
- COMB-03: Stacking produces a valid per-series forecast.
- COMB-04: HorizonAdaptive produces a valid per-series forecast.
- Docs half of the DoD satisfied: AutoEnsemble documented; every doc snippet run through the built extension.
</success_criteria>

<output>
Create `.planning/phases/04-autoensemble-surface-combination-methods/04-02-SUMMARY.md` when done.
</output>

